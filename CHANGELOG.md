# 更新日志

当前版本 **0.17.7**。安装包在 `dist/AllAi-Setup-<version>.exe`。用户日常从桌面快捷方式打开，改完 UI 必须 `npm run dist` 并完全退出后再开。

格式：最新在上。`0.4.7` 起按实际发版逐条记录；更早版本按已打出的安装包归纳。

---

## 0.17.7

**修掉 Codex 对话删不掉，并把额度预测从「危言耸听」拉回真实节奏。**

- **Codex 的对话删了又回来。** 根因是**一条 Codex 会话会被拆成多个 rollout 文件**（长会话会滚动分片），
  而删除只删了 `messagesFile` 那一个，剩下的分片下次扫描又按同一个会话 id 凑回一条 —— 看起来就像「删掉的对话自己回来了」。
  现在删的是整条会话的**全部分片**，并且一并清掉 `session_index.jsonl`。
  另外 codex 0.154 起 `codex delete` 在非交互终端会拒绝执行（要 `--force`）**却照样返回退出码 0**，所以命令行删除补上了 `--force`，
  并且成功与否一律以「文件真的没了」为准，不信退出码。回归：`node scripts/test-codex-delete.cjs`。
- **额度「预计用完时间」改用墙上时钟的平均速度。** 0.17.6 只挑「涨了的区间」取涨幅中位数，把睡觉、关机这些没在用的时间从分母里剔掉了，
  等于假设你 24 小时不停地按爆发速度跑：实测把「28.8 小时用掉 9%」外推成重置时 **135%**（真实约 53%），于是报「会提前两天用完」。
  现在按 **已用百分比 ÷ 窗口已经过去的时间** 预测 —— 已用百分比是窗口累计值，关机期间的消耗不会丢，没采样的时间也照样留在分母里。
  最近一天的节奏不再参与结论，只用来提示「最快可能什么时候用完」。只有**按平均也会超 100%** 才判定会提前用完。
**这一版还加了四个小功能：**

- **额度监控补上 5 小时窗口的账。** 以前「额度折合」「已消耗」只按周窗口算，5 小时那档被盖掉了。
  现在两个窗口都在时会多出「5 小时额度折合」和「这 5 小时已消耗」两张卡；只有周额度的账号（比如 Grok）不显示这一组。
- **服务和接口可以排序了。** 设置 → 模型与接口：聊天模型的服务列表、全局提供商、以及每个 Agent 自己的接口，**按住左边的手柄拖**就能排；键盘用户走悬停/聚焦时出现的上下箭头。
  聊天模型那边点一下就存（`PUT /api/providers`），全局和 Agent 的接口跟着各自的「保存」走。全局接口在 Agent 里不给排 —— 它的顺序归「全局提供商」那页管。
- **Agent 可以配图标了。** 设置 → 模型与接口 → Agent 接口，每个 Agent 都能挑预设图标、上传图片或填网址，左边列表里就显示它；
  留空则按这家 CLI 的品牌画（Claude Code 显示 Claude、Codex 显示 OpenAI）。图标存在 `prefs.brandIcons` 的 `agent:<id>` 键下。
- **溯源可以清空重测。** 设置 → 溯源多了「清空重测」，确认后清掉全部探测记录（统计、趋势、按型号都会归零），指纹库和自动探测开关不受影响。
- **Codex 官方直登的用量一直被当成中转，「周额度折合」永远算不出来。** 判定只认 `model_provider` 等于 `openai` / `chatgpt` 这几个字面值，
  但那是 `~/.codex/config.toml` 里**配置块的名字** —— 用户完全可以把官方直登的那一套叫 `custom`：
  `[model_providers.custom]` + `requires_openai_auth = true`，没有 `base_url` 也没有 `env_key`，走的就是 OAuth、吃的就是订阅额度。
  现在按配置内容判断：块里要 OAuth、自己没有 Key 和地址才算官方；配置里已经没有的名字，用**记住的旧判定**，再没有就看本机是不是 ChatGPT 登录。
  只要 Codex 是用 API Key 跑的，一律不算订阅额度。见过的判定会存进账本（`codexProviders`），中转站的配置块被删掉或改名之后也不会被误当成官方。
- **归属改成每次扫描都重判。** 以前只在会话文件有变动时才算，而改配置、重新登录都不会碰文件 —— 于是历史会话一直挂着旧结论。
  现在每轮扫描先重判归属（只读文件头，不重扫 token 账），改完配置下一次扫描就纠正。
- **溯源专区重做。** 以前只有一个呼吸灯加一串文字。现在有：结论卡（最近 50 次里几次对不上）、四个指标（累计探测 / 对不上 / 平均把握 / 覆盖型号）、
  近 14 天的相符与对不上堆叠柱状图（带图例和表格）、按型号的相符率和「最常被判成谁」、以及可展开的记录 —— 展开能看到**候选型号排名**、家族把握、几条回答有效。
  底部写明指纹库覆盖哪些型号、什么时候构建、判定全在本机完成。
- **溯源记录开始保存候选排名。** 指纹算法本来就算出了完整排名，之前只存了第一名。老记录没有这些，界面会直说。
- 指纹库对过了：和上游 ModelTrace 最新版（2026-09-05 的 astra 更新）**字节一致**，算法文件上游最后一次改动在我们移植之前，无需同步。
  上游此后的提交是他们自己的 Codex 插件和 Python 服务端的 403 修复，与 AllAi 无关。
- 回归：`node scripts/test-codex-delete.cjs`（6 项）、`node scripts/test-quota-monitor.cjs`（31 项）、`node scripts/test-usage-scan.cjs`（23 项）；界面要 dev server：`scripts/test-quota-monitor-ui.cjs`（23 项）、`scripts/test-model-trace-ui.cjs`（11 项）、`scripts/test-settings-order-ui.cjs`（7 项）。

---

## 0.17.6

**优化使用统计与官方额度监控。**

- 使用统计的时间范围收进一个按钮，二级面板左侧提供预设范围，右侧可以在日历中按分钟选择起止时间；结束时间早于或等于开始时间时不能应用。
- 额度预计用完时间改用窗口内多个有效采样区间的涨幅中位数，并要求至少观察到两次持续上涨，避免一次突发消耗把整周走势拉得过陡。
- ChatGPT 官方额度扫描会从 Codex 会话文件头重新确认 `model_provider`，升级旧缓存并兼容历史官方名称；AllAi 自己记录的官方聊天来源也会忽略空格和大小写差异，补齐本机 token 用量。
- 回归：`node scripts/test-quota-monitor.cjs`、`node scripts/test-usage-scan.cjs`。

---

## 0.17.5

**安装包改成向导了。** 以前双击就直接往里装，没得选；现在每一步都能自己定：

- **装给谁**：只为我，或者这台电脑的所有用户（选所有用户会要一次管理员权限）。
- **装到哪**：目录自己挑，默认 `%LOCALAPPDATA%\Programs\AllAi`（不用管理员权限）。
- **快捷方式**：桌面、开始菜单各一个勾选框，不想要就不勾。**以后自动更新也不会把它们装回来**（选择记在注册表里，静默更新时照着删）。
- **装完打开**：完成页上可以选装完直接运行 AllAi。

**自动更新还是全自动的，不会弹这个向导** —— 更新走静默安装（`/S --updated`，跳过所有页面），装完自己把 AllAi 拉起来。

## 0.17.4

**AllAi 自己能更新自己了。** 以前每次都要你下安装包、点安装、再开一次；现在改完由它自己在后台完成。

- **后台自动更新。** 启动 30 秒后查一次新版本（之后每 6 小时一次），查到就地后台下载 —— 走差分，通常只有几 MB，不挡着你用。
  **退出 AllAi 时会自动装上**，下次打开就是新版本。想马上用，设置 → 关于里点「立即重启并安装」。
- **设置 → 关于新增「AllAi 更新」。** 手动「检查更新」、下载进度、出错原因都在这里，也可以关掉启动时自动检查。全程不发弹窗（和本机 CLI 更新一个规矩）。
- 更新包从 GitHub Release 取（`JohnMuyuan/AllAi`），仓库地址由打包时写进 `resources/app-update.yml`，软件里不存任何密钥。
- 回归：`node scripts/test-app-update.cjs`。真连 GitHub、真下载、真装那一段要线上有更新的版本才跑得到。

## 0.17.3

**额度监控不再按 24 小时不停跑去吓你。** 以前虚线和「预计用完」取最近几小时和整周平均里较快的那个，晚上用一会儿就会画出一条直线，好像你接下来一直不睡。现在预测只按这周实际节奏：已用百分比 ÷ 已经过的时间，休息算在里面。最近速度还在，只是展示，不拿去外推。

## 0.17.2

**官方 Claude / ChatGPT 也能溯源。** 不一定要 API Key。官方登录走本机 CLI 发同一套数字挑战（会用一点订阅额度），不进你正在聊的那条会话。设置 → 溯源的手动检测里会出现 Claude 账号、ChatGPT 账号上的型号。Grok 指纹库没有，仍然不测。

## 0.17.1

**新专区：设置 → 溯源。** 用 [ModelTrace](https://github.com/xqy2006/ModelTrace) 的数字指纹，探测 HTTP 接口上的 OpenAI / Claude 是不是被路由到别的型号。

- 每条符合条件的聊天回复后自动测一次（一条挑战，不挡住回答）。对不上会发系统通知，并在答案前加黄条提醒。
- 概览：绿勾 0%、黄叹号 10% 档、红叉 20% 及以上；旁边是说明，下面是历史。
- 可关掉自动探测，也可对某个接口手动测三次。
- 官方登录的 CLI 账号、操控电脑循环、Agent 会话不测。指纹库随软件带上，方法归 ModelTrace（MIT）。

## 0.17.0

**新专区：设置 → 额度监控。** 用官方登录账号（Claude / ChatGPT / Grok）时，看额度消耗得多快、什么时候会用完、整周额度大概值多少，用来判断账号的健康情况。

- **消耗速度和预计用完时间。** 官方接口只给「这一刻已用百分比」，没有历史。AllAi 现在开着时每 5 分钟记一次（`~/.allai/quota-history.json`，留 45 天），
  据此算出每小时涨几个点、按这个速度什么时候到 100%、重置时会到多少，并给出健康判断（充裕 / 有点紧 / 会提前用完 / 马上用完 / 已用完）。
  速度取「最近」和「整个窗口平均」里较快的那个：只看最近，睡一觉回来就会说「永远用不完」；预警宁可偏早。
- **整周额度折合多少 token / 美元。** 同一个窗口里，本机记下的用量 ÷ 官方显示的已用百分比 = 整周额度，再给出「每 1% 约多少」。
  已用越多越准，界面标出可信度；已用不到 2% 不算（Claude 的百分比是整数，1% 时误差能放大几十倍）。美元按各家 API 公开价估算，不是账单。
- **每小时消耗、型号占比、走势。** 近 24 小时每小时的 token（悬停看花费、请求数、那一小时周额度涨了几个点）；当前周窗口里各型号的占比和明细表；
  周额度走势图带一条按当前速度往后推的虚线。每张图都能展开「查看表格」读到同样的数。
- **只算走官方账号的用量。** 本机 CLI 用量扫描现在按小时记账，并判断每个会话走的是官方账号还是中转站：Codex 看会话自己记录的 `model_provider`；
  Claude Code / Grok 的会话文件里不记，按它们的全局配置判断。判断不出来的不算，界面上写明排除了多少
  （比如本机 Grok 配了中转地址，Grok Build 会话就全部不计入，Grok 的折算宁可算不出来也不掺中转站的 token）。
- 升级后第一次启动会把**最近 40 天动过的**会话文件重读一遍，补上按小时的账；日统计的数字不变。
- **补上 0.16.38 那个测试开关的漏洞。** `ALLAI_NO_SUPPLIER_SCAN=1` 当时只挡住了启动时的导入，界面启动后自己调的 `POST /api/providers/scan` 没挡，
  临时测试目录照样会被导进本机真实的中转站和 Key。现在开关放在两条路共用的 `collectSuppliers` 里。
- 顺手修了使用统计页一处渲染时调用 `Date.now()` 的 lint 错误。

回归：`node scripts/test-quota-monitor.cjs`（25 项）、`node scripts/test-usage-scan.cjs`（17 项）、`scripts/test-quota-monitor-ui.cjs`（21 项，要 dev server，含「临时目录里没被导进 Key」「窄窗口型号名不截断」）。


## 0.16.50

- **修复缓存命中率超过 100% 的统计错误。** 兼容旧的 Anthropic 用量记录，把未命中输入、缓存读和缓存写统一成完整输入口径；统计卡片、按专区和官方窗口的输入合计不再低估。


## 0.16.49

- **使用统计支持自定义时间范围。** 可用日历选择起始和结束日期，并增加「当前」（今天）与「一天」（最近 24 小时）预设。


## 0.16.48

- **Agent / 全局接口：收起「模型」时，下面的添加框和思考档位一起收。**
- **AllAi 发 Agent 消息时，若这条会话已经在用户自己的终端里开着（Claude / Grok 登记了 pid），把字打进那个窗口，不再另起一份 `-p`。** 回复仍走会话文件，界面上的 watch 会跟上。找不到窗口就还是 AllAi 自己跑。

## 0.16.47

- **换图标时可以点选软件自带的预设。** 预设 SVG 在 `public/brand/presets/`。填地址会按关键词打分，多个都沾边时用最贴的那张（比如 aliyun.com 用阿里云而不是通义）。
- **思考过程展开后，新内容进来不会再自动合上，也不会把对话拽到顶部。** 改成按钮开合，不再用会乱触发的 `<details>`。
- **主界面不再重复显示版本号。** 版本只留在设置 → 关于。
- **启动画面跟日夜间。** 日间是浅底，夜间是深底；切过主题后下次启动还记得。
- **Agent / 全局接口的模型列表可以折叠。** 超过 8 个默认收起。

## 0.16.46

- **供应商自己的图标只出现在那条服务上。** 旗下模型仍按各自牌子显示，聚合站上 DeepSeek / 通义 / GPT 不会一起变成同一张图。
- **抓到过小或空的图会丢掉再找。** 1×1、空 SVG 那种「秒抓到的空白」不再当成功；同一波里挑最大的一张。

## 0.16.45

- **抓图标不再一条条排队等超时。** 站点根上的 favicon 并行去抓，接口路径（`/v1`）不再当网页读；谁先到用谁。
- **切提供商不会再串图标、也不会把网址带到下一家。** 每条服务自己的输入框和图标抓取，切走就取消进行中的请求。
- **「模型图标」里那组「接口」设置已拿掉。** 某一条服务的图标只在「聊天模型 / Agent 接口」里那条上配。

## 0.16.44

- **添加聊天服务、Agent 接口时可以直接配图标。** 填接口地址后会按网站自动抓 favicon；也可以贴图片网址或从本地选图。改图标就在这条服务 / 接口自己的设置里，不用再跑到「通用 → 模型图标」。第一次加入才自动抓，已经配过的不会覆盖。

## 0.16.43

英文翻译补完 + 图标能按「接口」配 + 几处之前漏掉的界面文案。

- **英文词典从 900 条补到 1004 条。**
  - **Model limits 的小字全翻了。** `lib/context-window.ts` 里那些说明（`Claude 4 及更早`、
    `GPT-5 / Codex（可输入部分）`、`国产长文模型`、`认不出型号，按保守值算`、`你手动设定的`）
    和 `lib/model-pricing.ts` 的（`Claude Opus 4.5 及之后`、`o 系列`…）之前一律显示中文。
    顺手把 ChatApp 里 `「${limit.note}（估算）」` 这种**拼好再翻**的写法改成模板 + 变量
    （`t("{note}（估算）", { note: t(limit.note) })`）—— 拼接出来的字符串是全新 key，词典永远对不上。
  - **Model icons 里的中文全翻了。** 厂商名（通义千问 / 智谱 GLM / 硅基流动 / 商汤日日新…）、
    那一大段说明文字，还有新增的「接口」「接口默认」两组标题。
  - **预设内容也翻了。** 模板名（通义千问→Qwen、智谱 GLM→Zhipu GLM、硅基流动→SiliconFlow）、
    官方登录的三个账号名和它们的提示语。模板应用时按**当前语言**把名称写进表单 ——
    存进 db 之后就不再跟着界面语言变（用户手改的也一样）。
  - 顺带补了一批零散文案：确认框（删除「X」？/ 移除「X」？）、通知（「X」做完了 / X 已登录）、
    压缩提示（`compactNotice` 拆成模板 + 变量，界面和服务端两条路文案一致）、导出 CSV 的表头、
    热力图的星期（一→Mo）、用量统计里的「自动识别」等等。
  - **没翻的**：发给模型的提示词（操控电脑那一整套）、斜杠指令的中文输入（`压缩`/`清空` 是输入不是显示）、
    `AgentTrace` 里用来**匹配**工具名的单字（`读`/`写`/`改`…）—— 翻了就匹配不上了。
- **可以给「接口」（模型提供方）配图标了。** 模型图标设置里多了一组「接口」，上传图片或填网址
  自动抓，配好之后这个接口下面的**所有模型**都换成它。
  图标查找优先级改成 **单个模型 → 接口 → 厂商**（`lib/brand.ts` 的 `iconFor`）：
  接口比厂商具体（一个中转站上挂着好几家），又比逐个模型配省事。
  接口的 key 是 `provider:<id>`，带前缀是为了不和模型 id / 厂商 id 撞车。
  接口行没配图标时显示一个中性占位（服务器图标），**不拿第一个模型的厂商图标冒充** ——
  OpenRouter 上挂着十几家，那么画是骗人的。
- **生图模型选择也有图标了。** `OptionSelect` 现在支持每个选项带图标，
  「聊天生图模型 / Agent 生图模型」两个下拉里，每个模型和列表里看到的是同一套图标。
- **使用统计「按模型」那张表加了模型图标。** 图标来自通用的「模型图标」（用户配的 + 认牌子），
  按型号认不出时再用接口地址兜底。为此给 `UsageStats` 传了 `providers` 和 `prefs`。
- **英文下 Agent 输入框不再把发送按钮挤到下一行。** 推理 / 权限两个下拉在英文下
  **只显示选中的档位**（`Reasoning · Balanced` → `Balanced`），图标（大脑 / 盾牌）已经说明是哪一项，
  完整名字仍留在 `aria-label` 里，读屏和自动化测试不受影响。
  另外给那行提示加了 `truncate`、给发送/停止按钮加了 `shrink-0`，宁可提示自己截断也别挤掉按钮。
- **聊天空状态显示当前模型的图标。** 以前永远是 AllAi 自己的标志，现在选中了模型就画那个模型的图标
  （盯着空白输入框时最先想知道的就是「我在跟谁说话」），没选模型才退回 AllAi 标志。

## 0.16.42

认牌子和配图标这两件事，重点补国内模型。

- **认得出国内模型了。** 原来 `lib/brand.ts` 只认 13 家（Claude / Grok / GPT / Gemini 这些
  国外为主），DeepSeek、通义、Kimi、智谱、豆包、混元、文心、星火这些**一个都不认**，
  模型名前面永远是空的，看着就像「没认出来」。现在扩到 28 家：
  国外加了 Google / Meta / Mistral / Cohere / NVIDIA / Microsoft，国内补了
  DeepSeek、通义千问、Kimi、智谱 GLM、豆包、腾讯混元、文心一言、讯飞星火、MiniMax、
  阶跃星辰、零一万物、百川、书生·浦语、商汤日日新、昆仑万维、美团 LongCat，
  外加 OpenRouter / 硅基流动 / Groq 三个聚合平台。
- **认得出聚合平台导出的 HuggingFace 风格 id。** 硅基流动 / 魔搭 / 火山方舟 / 阿里百炼
  导出来的不是干净的产品名，而是 `Qwen/Qwen3-235B-A22B`、`deepseek-ai/DeepSeek-R1`、
  `moonshotai/Kimi-K2-Instruct`、`zai-org/GLM-4.6`、`01-ai/Yi-1.5-34B-Chat` 这种
  `厂商/模型` 写法。规则改成按**厂商关键词**匹配，这类 id 现在都认得出。
- **认出来但没矢量图的牌子，画一个「品牌色 + 缩写」的色块。**
  以前只有 Anthropic / xAI / OpenAI 三家有矢量图，国内模型认出来了也什么都不画。
  现在新增 `BrandMonogram`：DeepSeek 是蓝底 D、通义是紫底 Q、Kimi 是黑底 K……一眼能分清是哪家。
  **认不出来的仍然留白** —— 画错牌子比不画更糟。
- **修 `MiniMaxAI/MiniMax-M2` 被认成 xAI。** 裸 `xai` 子串会命中「mini**maxai**」，
  而 `components/BrandMarks.tsx` 里**另抄了一份**认牌规则（`brandOfModel`），
  `lib/brand.ts` 修了它没修 —— 结果模型切换条上给 MiniMax 画了 **Grok 的 logo**。
  现在那份抄的规则删掉了，认牌子**只有 `lib/brand.ts` 一份**，`ModelSwitch` 直接用它，
  色块那一档也跟着生效（以前切换条里国内模型是光秃秃一个名字）。
- **修 `nvidia/llama-3.1-nemotron-70b` 被认成 Meta。** NVIDIA 的 Nemotron 是拿 Llama 改的，
  两个关键词都在，`llama` 规则排在前面就抢走了。现在 `nvidia` 规则排在 `llama` 前面。
- **修商汤 `SenseChat` / `SenseNova` 认不出。** 原来只有 `sense-\d`，认不到 `sensechat-5`。
- **图标导入：现在会读页面里的 `<link rel="icon">` 了。** 这是国内站点的关键 ——
  硅基流动、智谱、火山方舟、阿里百炼这些基本**不用** `/favicon.ico`，而是声明在
  `<link>` 里（还常常挂在 CDN 上）。只试标准路径要么 404、要么抓到一张糊的 16×16。
  实测 15 个国内 AI 站点：**13 个能抓到**，其中智谱走 `/static/images/favicon.png`、
  火山方舟走 `portal.volccdn.com`、阿里走 `img.alicdn.com`、混元走腾讯云 COS、
  Kimi 走 `statics.kimi.ai`、零一万物走 `apple-touch-icon.png` —— 这些以前全是抓不到的。
  支持 `icon` / `shortcut icon` / `apple-touch-icon` / `apple-touch-icon-precomposed` / `mask-icon`。
- **修「填了域名却报 google.com 连不上」。** 原来最后一个兜底候选是 Google 的
  `s2/favicons`，墙内**必然**超时（实测 10s），白等一轮还把错误信息写成了
  「www.google.com 连不上（超时）」—— 用户填的是 `qianfan.baidubce.com`，
  看到这句完全不知道发生了什么。现在分成两轮：先试用户自己的站（页面声明的图标 +
  标准路径，6s），再试第三方服务（4s），错误信息只提**用户填的那个域名**，例如
  「qianfan.baidubce.com 上没找到图标（返回 404）。可以直接填图片网址，或从本地选一张。」
  兜底服务换成国内能通的 `favicon.cccyun.cc`，Google 降到最后且只给 4 秒。
  失败路径实测从 ~14s 降到 ~5s，成功路径 255–323ms。
- **设置里填完域名可以直接粘贴。** 图标输入框支持 `onPaste` 自动去抓，不用再点一下按钮。
  状态文案跟着改：认得出但没矢量图的显示「品牌色块」而不是「未识别」。

## 0.16.41

接手后的第一轮审查，改的全是**异常路径和清理**，正常用时看不见，但踩上就是「卡死」或「越来越卡」。

- **修「发送之后界面再也发不出消息」。** 两条路的 IPC 调用都没有兜底：
  官方登录聊天的 `await desktop.officialChat()` 在 HTTP 那条 `try/finally`
  **外面**，一抛异常（终端宿主没起来、宿主中途掉线、invoke 超时）就直接跳出
  整个发送函数，`finally` 里的 `streamingRef.current = false` 永远执行不到 ——
  界面一直转圈，之后每次发送都被 `streamingRef` 挡掉。`sendAgent` 的
  `firePrompt` 同样没有兜底，`agentStreaming` 和 `markRunning(true)` 都收不回来，
  而它开头那句 `|| agentStreaming` 会让**这条工作**再也发不出消息。
  只能重启软件。现在异常接住并走原来的失败分支：官方登录写进气泡、
  Agent 写进这一轮的 trace，**不弹窗**（约定 22）。
- **修快速切换对话 / 工作时的错位。** `selectConversation` 和 `selectWork`
  拿到结果后不校验还是不是当前这条。大对话 / 长会话加载慢，点了 A 又马上点 B 时，
  A 的结果后到会把界面整块换成 A 的内容（侧栏还高亮着 B），
  连模型和思考档位都被改成 A 的。现在用 ref 记住最近一次点的是哪条，不是它就丢掉。
- **修 `electron/watch.ts` 建到一半失败时漏关 watcher。** 目录 watcher 建起来之后，
  `filesToWatch()`（codex 那条要读目录）抛错时，catch 只返回 `{error}` 就结束了 ——
  调用方拿不到 `close()`，那个 `fs.watch`、已建的文件 watcher 和防抖定时器全部永久泄漏。
  正是约定 66 踩过的同一类坑。现在 catch 里收干净。
- **修终端宿主的 `chats` 只增不删。** 每一轮 CLI 调用（Agent 发一条、官方登录聊一轮）
  都往 Map 里留一个句柄（子进程引用 + 输出缓冲 + StringDecoder），只有显式 kill 才删，
  正常跑完的一直挂着，一天下来几百份，全在长期不重启的宿主进程里。
  现在一轮 `done` 就撤掉 —— done 说明进程已经 close，之后再收到 kill 是 no-op。
- **修远程控制的 `onRelay` / `handleRpc` 没有 catch。** 两个都是 async，
  里面的 await 一抛就是未处理 rejection：手机会停在某条工作的界面上却再也收不到新消息，
  而这边的 ping 心跳还以为连接是好的。现在接住并写进 `~/.allai/desktop.log`。

## 0.16.40

- **英文界面覆盖到设置、对话框、统计、创作、远程和手机网页。** 词典仍用中文原文当 key（`lib/i18n.ts`），`LangProvider` 挂在窗口最外层，标题栏和确认框也会翻。权限/推理档位、斜杠指令的中文名和用法跟着语言走。用量页在英文下不再跟「约 N 万/亿」。手机网页同一套词典，已 `remote:deploy`。发给模型的提示词、斜杠中文别名、已经写进对话里的旧句子仍是原文。

## 0.16.39

- **补充英文界面词典覆盖。** 增加统计、电脑操控、联网、通知、Agent 提问和关于页等常用文案的英文翻译。
- **移除主界面左下角主题切换。** 主题切换统一放在设置 → 通用 → 外观。


## 0.16.38

- **界面能切英文了。** 设置 → 通用 → 语言，中文 / English / 跟随系统三档（跟随系统按
  `navigator.languages` 判断，不是中文就走英文）。词典在 `lib/i18n.ts`，**用中文原文当 key** ——
  代码里读着就是最终文案，没翻的自动回落中文，界面不会出现空白或 `missing.key`，
  原文改了就是新 key，漏翻会直接看见。这一版先覆盖外壳：侧栏、输入框、Agent 顶栏、
  设置页签和外观/语言两栏；设置里各个分页的正文还是中文，后面接着补。
  ⚠️ 协议标记（`ANSWER_MARK` / `HANDOFF_MARK`）、斜杠指令的中文别名、db.json 里的枚举值、
  发给模型的提示词**都不进词典** —— 翻了会直接弄坏功能，`lib/i18n.ts` 开头写死了这条。
- **深浅色改成三档：日间 / 夜间 / 跟随系统。** 原来只有一个开关，存的是最终颜色；
  现在存的是**模式**（`allai-theme`: light / dark / system），选「跟随系统」之后在 Windows 里改深浅色，
  AllAi 当场跟着变（订阅 `prefers-color-scheme`）。老的 light / dark 值照旧认，不用重设。
- **Agent 顶栏窄窗口时不再半截换行。** 标题长 + 窗口窄的时候，原来是「接续到新对话」自己掉到第二排、
  换模型还硬留在第一排。现在换模型和右边那排按钮绑成一组：要么整组待在标题右边，
  要么整组掉到第二排、换模型在最左边。标题该截断就截断，不会把按钮挤变形。
- **跑回归时不再把真 Key 导进隔离目录。** AllAi 每次启动都会扫本机 CLI 配置、把中转站和 Key
  导进 `db.json`（对用户是省事）—— 但拿一个临时 `ALLAI_DATA_DIR` 跑测试时，这个目录里就白白躺了一份
  真凭据。现在 `ALLAI_NO_SUPPLIER_SCAN=1` 能关掉这一步，测试脚本的用法说明里也写上了。


## 0.16.37

- **大数字旁边补一个中文写法。** K/M/B 是三位一级（千 / 百万 / 十亿），中文是四位一级（万 / 亿），
  两套错开，「3.16B 到底是几亿」得心算一下。现在统计页的概览卡片、三个专区的总量、
  按型号那张表的合计、以及热力图的共计 / 日均 / 单日最高，都在数字后面跟一个
  「≈ 32 亿」「≈ 853 万」这样的小字。
  1 万以下不显示（「0.3 万」比原数还难读）；10 以下留一位小数（3.1 亿），10 以上取整（69 亿）。
  写法在 `lib/format-count.ts` 的 `formatCountCN()`，和原来的 `formatCount()` 一处管。


## 0.16.36

0.16.35 那份统计**数字是错的**，三处口径问题，都在这一版修掉了：

- **Claude 的一次请求被算了将近两次。** Claude Code 把**一次 API 响应的多个内容块拆成多行**写进会话文件，
  每行都带同一份 usage。按行累加就重复计了 —— 实测这台机器 11003 行里有 5009 行是这种（虚高 1.83 倍）。
  这些重复行永远相邻、usage 完全相同，现在记住上一条的 `requestId` 就跳过。
  本机请求数因此从 14744 修正到 9903。
- **Claude 的缓存读整个漏掉了。** Anthropic 报的 `input_tokens` **不含**缓存读写（实测 input=2、
  cache_read=33849），而 Codex / Grok 的**含**。两种意思混在同一张表里，合计（输入+输出）就成了笑话：
  Claude 那几十亿缓存 token 凭空消失。现在统一口径 —— **`input` 一律表示「送进去的全部输入」**，
  缓存读写是其中的明细。聊天那条路（`chat-parse`）也一起统一了。
- **导入的 CC Switch 数据是第三种口径。** 它存的是「纯新增输入」，缓存另算
  （验证过：codex 某天 2.183M×$5 + 0.261M×$30 + 79.23M×$0.5 = $58.3，和它自己记的 $58.36 对得上）。
  导入时也补齐成同一口径。

修正后（这台机器）：近 7 天 1.45B、近 30 天 3.14B、全部 6.90B（2026-05-15 起，44923 次请求）。
按来源：Claude Code 3.54B / Codex CLI 2.45B / Grok Build 0.63B / OpenCode 0.28B。

- 回归 `scripts/test-usage-scan.cjs` 补了「同一次响应拆成的多行只算一次」和「input 补齐成全部输入」两项。


## 0.16.35

- **使用统计现在算这台电脑上所有 Agent 的消耗**，不管那一轮是 AllAi 发的、你在终端里自己跑的、
  还是别的壳子（CC Switch、IDE 插件）跑的。做法和 CC Switch 一样：**增量读各家 CLI 自己的会话文件** ——
  Claude Code 的 `message.usage`、Codex 的 `token_usage_record`、Grok 的 `turn_completed.usage`。
  实测这台机器 220 个会话文件（420MB）首轮扫完 1.6 秒，之后增量 18 毫秒；每 5 分钟补扫一次，
  Agent 每跑完一轮也顺手补一次。设置 → 使用统计里有「立即扫描」。
- **能把 CC Switch 的历史用量导进来了。** 读它 `~/.cc-switch/cc-switch.db` 的日汇总表（**只读，不改它任何东西**），
  这台机器上是 2026-05-15 → 2026-08-12、78 天、35,020 次请求、$4499.48。重复点不会翻倍（整份替换）。
  同一天同一来源本机已经扫到的，以本机为准，不会重复计（实测两边几乎不重叠：CC Switch 到 08-12，
  本机会话文件从 08-16 才开始 —— CLI 自己把更早的清掉了）。
- **几万次请求不会撑爆 usage.json。** 扫描和导入的数据按「天 × 来源 × 型号」汇总，
  存在新的 `~/.allai/usage-rollups.json`（实测 88KB），读的时候折成统计页认识的事件
  （49,764 次请求只变成 268 条）。AllAi 自己这边（聊天、创作）仍然一条一条记在 `usage.json`。
- **Agent 的用量不再在界面里单独记一份**，改由扫描器统一算 —— 两边都记会翻倍
  （界面拿到的是一轮的合计，会话文件里是同一轮每次请求的明细）。老记录不删，读的时候跳过。
- **花费**：Grok 自己报（`costUsdTicks`），CC Switch 导入的带它算好的钱，这两部分是真实的；
  Claude Code / Codex 的会话文件里没有钱，按 `lib/model-pricing.ts` 的型号单价**估算**，
  界面上写明了只能看量级。
- 新增回归 `scripts/test-usage-scan.cjs`（12 项）：三家的字段口径（Codex 的 input 已含缓存读、
  Grok 的一轮总和）、AllAi 自己的官方聊天会话不算进 Agent、追加写只读新增那段、文件被重写时整份重算。


## 0.16.34

- **修：电脑上换 Agent 对话，手机右上角的进度条跟着变。** 手机看的是**它自己**打开的那条工作
  （只有 `agent.open` 是按手机分开处理的），可 0.16.33 加的上下文进度条发的是**电脑选中那条**的数 ——
  电脑上一换对话，手机那一格就跟着跳，而手机的消息还停在原来那条。现在占用取这台手机自己盯的
  那条会话文件里 CLI 报的数，窗口大小它没报就用这条工作型号的表值。
- **同一个毛病「型号」也有，一起修了。** 手机上显示的是电脑那条工作的型号；更糟的是
  **在手机上换模型会改到电脑选中的那条工作上去**。现在型号按手机自己那条显示，
  换模型也带上工作编号、改的是它自己那条（推理强度和时间线提示只在改的是当前这条时才动）。
- 回归 `test-remote-sessions` 扩到 9 项，新增三条专门盯这件事：电脑选中 work-2、手机打开 work-1 时，
  activeId / 型号 / 上下文占用都必须是 work-1 的。修之前后两条会挂（拿到的正是电脑那条的数）。


## 0.16.33

- **电脑上的 AllAi 关掉后，手机整屏换成「已断开」。** 以前只在顶栏挂一条小字，底下还是上次同步下来的
  旧界面 —— 看着像好的，点什么都报错。现在写清楚：连接已断开、Remote Control 会不断尝试重连、
  想配别的电脑就扫二维码；底下再给「扫码配对其他电脑」和「换一台已配对的电脑」两个入口，
  底部页签这时收起来（点了只会报错）。电脑回来自动恢复，不用手动重连。
  只闪一下的重连不会整屏跳掉：中继明确说电脑不在线才立刻换，单纯连不上要拖过 4 秒。
- **Agent 在手机上向你提问了。** Claude Code 的 AskUserQuestion 以前只在电脑上有卡片，
  传到手机时那个 `ask` 被丢掉了 —— 你在手机上只看到一行「问用户」，**整条工作卡在那儿等，
  只能回电脑上答**。现在手机上也是卡片：选项能点、多选能勾、还能自己写「其它」，
  发出去的回答和电脑上一字不差（拼装挪进 `lib/agent-answer.ts`，两边共用一份），
  同样不显示成你的消息。
- **手机 Agent 输入框也能打 `/` 了。** 和电脑上同一份目录（0.16.31），弹出这家 CLI 认识的指令，
  带中文名和用法，点一下填进输入框。
- **手机上能看到上下文占用了。** 聊天和 Agent 的顶栏各有一条小进度条 + 百分比，
  快满了会变黄变红 —— 以前只能等它突然开始压缩。
- 远程端到端回归扩到 27 项（新增断开界面 4 项、上下文占用 1 项）；
  `test-remote-sessions` 扩到 6 项，新增「提问（ask）传到手机」和「改文件的差异不跟着发」。


## 0.16.32

- **删掉聊天对话时，它的图片和附件也会一起删掉了。** 删创作一直会顺手删掉它生成的图，
  **删聊天却什么都不删** —— 你发过的每一张图、AI 在聊天里生成的每一张图，都永远留在
  `~/.allai/uploads`，而且没有任何入口能清。实测这台机器上 67 个附件 4.88MB **全部**
  没有任何对话或创作在引用。
- **开机会清一次没人引用的附件**，把以前攒下的那些收掉。很保守：
  - 还被别的对话、创作产出或人物照片引用的，一个都不删（一张图可以「用作参考」带进创作）；
  - 正文里 `![图](/api/uploads/…)` 这种 markdown 链接也算引用 —— 聊天里生成的图就在这儿，
    它不在 `attachments` 里；
  - **有任何一条对话文件读不出来就整轮不做** —— 扫不全还敢删，等于把那条对话里的图清了；
  - 24 小时内动过的不碰（可能是刚传上来、还没写进消息的）；
  - 只剩一条索引、文件早没了的，清掉索引。
- 新增回归 `scripts/test-upload-cleanup.cjs`（7 项，全程用临时数据目录）：重点测的是安全阀 ——
  被别处引用的不许删、扫不全时一个都不删、太新的不碰。先确认它在修之前会挂。


## 0.16.31

- **Agent 输入框打 `/` 会弹出当前这家 CLI 的指令列表。** 每条有中文名和用法，↑↓ 选择、Enter 填入、Esc 关闭。Claude / Grok / Codex 各显示自己认识的那一句（例如 Codex 清空是 `/new`）。中文 `/压缩` `/状态` 也能筛到。

## 0.16.30

- **Agent 输入框里的斜杠指令按各家 CLI 翻译后再发。** `/压缩`、`/状态`、`/清空`、`/帮助`、`/用量` 等会变成 Claude 的 `/compact` `/context` `/clear`，Grok 的 `/session-info` `/usage`，Codex 的 `/new` `/status`。指令不再拼上 Skills、推理强度和历史。
- **同一模型和 CLI 到量时，先跑这家自己的压缩指令再发你的问题。** 不再用 AllAi 另写一份摘要、另开会话。对话里仍显示压缩进度。

## 0.16.29

- **同一模型和 CLI 到量压缩，留在当前对话里，并显示进度。** 不会在侧栏另开一条。对话里会出现「压缩上下文」：先写正在整理前文，再实时跳摘要已写多少字，最后换成压完说明。底下的 CLI 会话号会换（旧窗口已经满了），扫描不会再把它列成第二条。

## 0.16.28

- **同一条 Agent 里换后端型号，不再改去另一家 CLI。** 在 Claude Code 工作里选 Grok 4.6，仍然跑 `claude`，只换这一轮的网关和 `ANTHROPIC_MODEL` / `--model`（和 CC Switch 一样）。全局接口上的 grok / gpt 型号会显示在当前 Agent 的选择器里。没超窗口就续当前对话。

## 0.16.27

- **换模型不再自动开一条新对话。** 没超过新模型的上下文上限，就留在当前这条里继续发（并带上 `--model`）。只有窗口真的装不下，才压缩并换 CLI 会话；切到另一家（Claude → Grok）也还是这一条工作，侧栏不会多出一条。旧会话文件会藏起来，避免扫描又把它列出来。

## 0.16.26

- **修：在 Claude Code 的工作里选 Grok 4.6，压缩开出的新对话仍是 Opus 5。** 这不是权限问题。一条 Agent 工作绑在一家 CLI 上，以前换模型只改 `--model`，还是跑 `claude` —— 它不认识 grok-4.6，就继续用默认的 Opus 5。现在按型号判断该用哪家：选 Grok 就开 Grok Build 的新会话，选 Claude 才继续 Claude Code。选择器里也能看到另外两家的型号。

## 0.16.25

- **修：在 AllAi 里换了模型，Claude Code 仍用旧型号。** `--resume` 会无视 `--model`。以前只有「这一轮已经从 AllAi 发过」才知道会话绑的是哪个型号，从侧栏打开一条历史再换模型会继续续旧会话。现在换模型会开新会话，并把实际在用的型号记下来。
- **修：从 Claude（1M）切到窗口更小的模型时，历史原样塞过去。** Grok 自己压上下文会另开一条会话，出现在左边列表里。换模型时按**新模型**的窗口压（装得下的 35% 留原文，更早的做成摘要），不再拿 Claude 报的 1M 上限来判断。
- Grok 报 `grok-4.6` 不存在，是账号/CLI 里没有这个型号，不是上下文的问题。设置 → Agent 里同步一次官方模型。

## 0.16.24

- **修：Claude Code 从官方账号切到全局 API Key 会报 connectors 被关掉。** 本机还留着 claude.ai 登录，AllAi 又把 `ANTHROPIC_API_KEY` 和 `ANTHROPIC_AUTH_TOKEN` 一起塞进这一轮，Claude Code 就把冲突警告写进对话。现在这一轮只带这一种鉴权：第三方网关用 Token，官方 Key 用 API Key；父进程里残留的 Key 先清掉；并且明确关掉 claude.ai connectors（本地 MCP 不受影响）。切回官方登录仍然用本机账号，不用退出登录。

## 0.16.23

- **关掉终端之后不会再显示「运行中」了。** Claude Code 有两种判活方式：它自己写的登记表
  （`~/.claude/sessions/<pid>.json`，带 `sessionId` 和 `status: busy/idle`，进程退出就删），
  以及「会话文件 3 分钟内被写过」这个猜测。以前两者是 **或** 的关系，而且没登记时只剩猜测 ——
  你直接关掉终端窗口，进程早没了，AllAi 还会照着文件时间继续说「运行中」整整三分钟。
  现在对 Claude 以登记表为准：**没登记就是没在跑**，`idle` 就是在线但没在跑；
  只有登记表整个读不出来时才退回猜。Grok / Codex 没有这种登记表，仍然按时间猜。
- **AllAi 自己跑的 Agent 一轮，按下发送立刻显示「运行中」。** 以前得等下一轮扫描（4 秒）
  才反映出来 —— 可这一轮本来就是 AllAi 自己 spawn 的，它当场就知道。现在直接标上，
  顶栏写「运行中 · Claude Code」、侧栏绿点同时亮；这一轮结束、出错或你手动停止就撤掉。
- 新增回归 `scripts/test-agent-live.cjs`（8 项，用临时 HOME 造登记表，不碰真的 `~/.claude`）：
  残留登记不算数、busy 算运行中、idle 只算在线、关掉终端后不再是运行中、Grok 仍按时间猜、
  登记表读不到时退回猜。`test-handoff-summary.cjs` 扩到 16 项，加了「立刻显示运行中」和「结束后撤掉」。


## 0.16.22

- **接续不会再凭空多出一条 Agent 对话了。** 整理摘要那一轮本来就该续当前会话，可它触发的时机
  恰恰是上下文快满的时候 —— 那正是「该压缩了」的条件，于是按常规逻辑另起了一条 CLI 会话：
  整段历史被重放一遍（又慢又贵），CLI 在磁盘上多出一个会话文件，Agent 列表里就凭空多一条对话。
  现在这一轮强制续原会话（`keepSession`），不重放、不新建，自然也就没有要善后的临时对话。
- **交接用的整理请求不会再跑到别的 Agent 对话里了。** 它是这条工作里真实的一轮，会落进 CLI 的
  会话文件；而这条工作以后每开一条新会话，AllAi 都会把历史原样重放进去 ——「请写一份交接摘要…」
  就这么被复制到别处，模型还可能照着它再写一份摘要。现在它带一个标记 `〔AllAi：生成交接摘要〕`：
  - 换会话重放历史时，**这一轮连同它写的摘要一起丢掉**（摘要已经原样交给新对话了，重放是纯噪音）；
  - 界面和手机上都不显示成你的消息（和「回答提问」那条一个做法）。
- 回归 `scripts/test-handoff-summary.cjs` 扩到 14 项，新增：整理请求续的是原会话、没另起新会话、
  带标记、不显示成用户消息，以及重放历史时整轮被剔掉。


## 0.16.21

- **交接摘要不会再变成上一轮的普通回复。** 之前它取的是「会话文件里最后一条 assistant」。
  交接常常要开一条新的 CLI 会话（上下文满了、换了模型），而这条工作本地记的 `messagesFile`
  要等下一次扫描才更新 —— 那几秒读到的是**上一条会话**，最后一条 assistant 就是上一轮的普通回复。
  于是「交接摘要」变成一段「改好了，已经打成 0.16.13……晚安，辛苦啦～」。
  现在只认这一轮自己的产出，三道来源往下退：这一轮的事件流 → 会话文件里**新增**的助手消息
  （按开始前的 id 集合筛）→ 都没有就报错并把面板留在「出错」，**绝不拿不相干的回复顶上**。
- **「复制恢复命令」按钮开过一次新会话就再也不回来了。** `sendAgent` 开新 CLI 会话时会把这条工作
  标成 `source: "new"`，但 CLI 把会话文件写出来之后它就不是新的了 —— 定时扫描会把它当 history 扫回来。
  `mergeAgentWorkLists` 从不合并 `source`，于是它永远停在 "new"，`canResume()` 一直为假。
  换模型、自动压缩上下文、刚接续出来的工作都会中招。现在扫到了就把 `source` 接回来。
  （顺带说清楚：**重命名本身不会让这个按钮消失** —— 它完全不碰 `cliSessionId` / `source`，
   实测重命名前后按钮都在。真正的触发点是上面这条。）
- 新增两个回归：`scripts/test-handoff-summary.cjs`（真界面：会话文件里永远只有旧回复，
  摘要必须来自这一轮）和 `scripts/test-work-merge.cjs`（扫到之后 `source` 回到 history、
  `canResume` 变真）。两个都先确认在修之前会挂 —— 前者原样复现出了那段「晚安，辛苦啦～」。


## 0.16.20

- **修掉 `spawn ENAMETOOLONG`。** 0.16.19 让大 payload 能送到终端宿主了，下一层就露出来：
  宿主把整段 prompt 当成**命令行参数**交给 CLI。Windows 整条命令行上限 32767 个字符，
  而 Agent 换会话要把整段历史重放过去 —— 实测一条普通长度的历史算出来 **62121 字符**，将近两倍。
  代码里本来就有「太长就写临时文件」的机制，但只有 Grok 真的用上了：
  **claude 那一支把文件读回来又塞进 argv**（等于白写），codex 压根没管。
  现在三家各走各的入口，都对着本机 CLI 的 `--help` 核过、并且实跑验证过：
  - Grok：`--prompt-file <文件>`
  - Claude：`-p` 不带参数时从 stdin 读
  - Codex：`codex exec` 省略 PROMPT 参数时从 stdin 读（**不能又给参数又给 stdin**，
    那样 stdin 会被当成额外的 `<stdin>` 块追加上去）
  短 prompt 照旧走 argv，常见路径没动。
- 新增回归 `scripts/test-cli-args.cjs`（17 项）：断言长 prompt 不出现在 argv 里、命令行长度、
  三家各自的入口、短 prompt 不受影响，最后真 spawn 一个假 CLI 确认 stdin 这条线是通的。
  同样先确认它在修之前会挂（并且能量出那 62121 字符）。


## 0.16.19

- **真正修好「无法联系终端宿主」。** 0.16.18 猜错了方向。日志（那一版加的）给出了答案：
  没有任何 stderr、没有 spawn 失败、没有异常 —— 宿主根本没出事。
  **`child.send()` 的返回值是背压，不是成败**：队列攒到约 256KB 它就返回 `false`，而消息照样送到
  （实测连发 5 条、其中两条 500KB/2MB 返回 false，子进程 5 条全收到，`connected` 一直是 true）。
  `pty.ts` 从第一版起就把 `false` 当成「联系不上」直接报错。
  所以只有 `agent:prompt` 会中招 —— 它带着整段历史，Claude Code 续上下文时轻松几百 KB；
  `list` / `attach` / `kill` 那些小消息永远碰不到。**会话越长越必然失败。**
  现在只认 send 回调里的 error，返回值不再参与判断。
  （0.16.18 的「发不出去就重起宿主再试」在这个情况下反而更糟：消息其实已经送到了，
   却把正在干活的宿主杀掉重发一遍。一并改掉。）
- **Agent 选的模型重启后不会再退回去了。** 选择以前只活在内存里：`works` 每次启动都是重新扫
  CLI 会话文件得来的，里面的 model 是**上一轮 CLI 实际用的**那个。只有「选完又发过一轮」
  才碰巧能从会话文件里读回来；选完没发（或者发失败了 —— 正好就是上面那个 BUG）就丢。
  现在存进 `agentWorks` 覆盖层（和改名、隐藏、压缩同一层），启动时叠回去。
- 新增两个回归：`scripts/test-pty-bridge.cjs`（背压不算失败、真断了才报错）和
  `scripts/test-agent-model-persist.cjs`（选模型 → 不发送 → 重开窗口，选择还在）。
  两个都先确认在修之前会挂。


## 0.16.18

- **修掉「无法联系终端宿主」。** 终端宿主是启动本机所有 CLI 的那个子进程（Agent、官方登录聊天全靠它），
  它起不来就什么都跑不了。三处一起改：
  1. **安装包里根本没带它的依赖。** electron-builder 会把 `extraResources` 里的 `node_modules` 过滤掉，
     `standalone` 早就在 `after-pack` 里补回去了，`pty-host` 漏了 —— 它 `require("node-pty")`，
     少了就一启动就挂。在开发机上看不出来：`resources/pty-host` 就在仓库里面，
     require 会一路往上找到仓库自己的 `node_modules`；换台机器（真正装安装包的人）直接废掉。
  2. **宿主崩了不会自愈。** 判活只看 `killed`（那只表示「我们给它发过信号」），
     宿主自己崩了这个值还是 false，之后每次调用都撞同一堵墙。现在看 `connected`，
     发不出去就丢掉重起一个再试一次。
  3. **崩溃原因被扔了。** 宿主的 stderr 是 `ignore`，挂了一个字都不留，只剩界面上那句没头没尾的报错。
     现在写进 `~/.allai/desktop.log`，报错本身也会告诉你去哪看。
- **打包检查加了一条，并且是在仓库外面跑的。** `check-packaged-runtime.cjs` 现在把
  `resources/pty-host` 拷到临时目录再启动、真发一次 IPC —— 就地跑会顺着仓库找到依赖，怎么都过，
  上面第 1 条就是这么漏出去的。
- 日志读写统一到 `electron/log.ts`（路径、`ALLAI_DATA_DIR`、4MB 轮转一处管）。


## 0.16.17

- **「接续到新对话」的进度面板重做，这回是真能看的。** 点下去立刻弹出面板：当前工作、当前模型、当前阶段、
  已写了多少字摘要、**执行到哪一步**（模型这一轮调了哪些工具、在想什么，一条条往下长）、已运行多久，
  底下一个「停止接续」。关掉面板后台照跑，顶栏按钮变成「查看进度」，点一下面板就回来。
  跑完面板不会自己消失，会变成「接续完成」并给一个「打开新对话」的入口；失败或被你停掉也会留在面板里说清楚原因。
- 0.16.15 的面板有四个问题，全修了：
  1. **「查看进度」是禁用的** —— 接续期间那条工作正在跑，按钮跟着 `streaming` 一起灰掉，
     等于面板一关就再也打不开，只能干等。
  2. **「已运行 N 秒」不会走** —— 这一轮是静默跑的，界面上没有别的东西在变，秒数就停在打开面板的那一秒，
     看着像卡死了。现在面板自己每秒走一格。
  3. **一切走就没了** —— 面板挂在 Agent 工作区里，切到别的工作或别的页面就被卸载。现在挂在最外层，切哪都在。
  4. **进度只有三句写死的话** —— 现在接的是模型真实的事件流，而且切到别的工作也照样收得到。
- **修掉静默那一轮把旧工作最后一条回复弄脏。** 交接摘要不新建气泡（约定 63），但它的正文和工具调用
  还是会落到**上一轮**那条回复后面，几秒后才被会话文件覆盖回来 —— 中间那几秒用户是看得见的。
- **摘要从这条工作自己的会话文件里取。** 之前读的是「界面当前这条工作」的消息：面板关掉、人走去看别的工作，
  拿回来的就是别人的回复，会把不相干的内容当成交接摘要接过去。
- 新增界面回归 `scripts/test-handoff-ui.cjs`：真界面 + 真 preload + 假 IPC，19 项。


## 0.16.16

- **手机切后台再回来，Agent 页不会再「卡住不动」。** 手机锁屏或切走会断开连接，回到前台是一次重新握手。
  之前电脑那边会当成一台全新的手机，把「这台手机正在看哪条 Agent 工作」忘掉 —— 手机还停在那条工作的界面上，
  却再也收不到新消息，得退回列表重新点一次才行。现在重新握手会自动接回原来那条。
- **修掉远程控制的文件监视器泄漏。** 手机打开一条 Agent 工作时，电脑会盯住对应的会话文件。
  掉线、重连、重新握手时旧的监视器没有被关掉，只是丢掉了引用：手机每切一次后台就多漏一份，
  漏到后来 Agent 每写一次文件就有一堆解析同时跑，电脑会越用越卡。
- **日志不再无限增长。** `~/.allai/desktop.log`（Next 服务的全部输出）和 `remote.log`（远程操作审计）
  以前只增不减，装久了会在用户目录里长到几百兆。现在超过 4MB / 1MB 就轮转，各留一份上一轮的。
- **`desktop.log` 跟随 `ALLAI_DATA_DIR`。** 之前它固定写 `~/.allai`，隔离的测试实例会往真实数据目录里写日志。
- 新增回归测试 `scripts/test-remote-sessions.cjs`：不用中继和窗口，直接驱动真实的 `remote.js` 验证上面两条。


## 0.16.15

- **接续到新对话增加后台进度面板。** 点击后显示当前模型、阶段、详情、运行时间和停止按钮；关闭面板不会取消任务，原按钮变为「查看进度」。


## 0.16.14

- **接续到新对话改为后台生成交接摘要。** 不再向当前 Agent 时间线插入一条伪装成用户消息的整理请求；摘要完成后仍填入新对话输入框，用户确认后再发送。


## 0.16.13

- **回答 Agent 的提问不再以你的名义出现。** 0.16.12 里点提问卡片的「发送回答」，会多出一条像是你自己打的消息（「关于……，我的回答是：……」）。
  现在和 Claude Code 终端里一样：回答照样发给 Agent，它接着干活，但界面上不显示成你的气泡，只在那张提问卡片里写「你的选择：……」。
  从会话记录读回来的历史、手机上、左侧快速跳转也都不把它当成你说的话。没点卡片、自己打字继续的，卡片标「已继续」。

## 0.16.12

- **左边 Agent 列表能看出「接续」关系。** 一条接续链画成一组：最早那条是主对话，照常显示，右边标「共 N 段」和一个折叠箭头；
  接续出来的对话挂在它下面 —— 缩进、左边一条竖线、前面一个 ↳，写着「接续 1 · Claude Code」「接续 2 · …」。
  接续的接续也拉平到同一组，按先后排。整组按组里最新的一次活动排序，接续出来的新对话不会单独跑到别处去。
  你正在看的对话在哪条链里，哪条就自动展开；其它默认收起，手动展开 / 收起会记住。搜索时不分组，免得搜到的对话被收起来。
- **Claude Code 向你提问时，AllAi 会画出提问卡片。** Claude Code 的 AskUserQuestion 以前在 AllAi 里只是执行过程里的一行「问用户」，
  问题和选项都看不到（AllAi 用非交互模式跑 CLI，没有终端让你回答）。现在回复下面会出现卡片：标题、问题、每个选项和说明，
  多选题可以勾多个，也可以自己写「其它」；点「发送回答」就把回答作为下一条消息发出去，Agent 接着原来的会话干活。
  回答过的卡片变成「已回答」。执行过程里那一行也带上了问题。

## 0.16.11

- **新功能：Agent「接续到新对话」。** 对话太长、上下文快满时，点 Agent 顶栏的「接续到新对话」：
  1. 先让这条工作正在用的模型把进度写成一份交接摘要（任务目标、已完成、改过的文件、还没做完 / 下一步、你提过的要求、要注意的地方）——
     它最清楚自己做过什么，这一轮会留在旧工作里，随时能翻；
  2. 写完后在同一个目录、同一个 Agent 和模型下开一条新工作；
  3. 「接续自《旧标题》」加上交接摘要放进新工作的输入框，**不会自动发送**，你看过、改过再发。
- 两条工作互相链接：新工作顶上是「接续自《…》」，旧工作顶上是「已接续到《…》」，点标题就跳过去；重启后也还在。
- 上下文快满时，输入框上方会提醒一句，旁边就有「接续到新对话」按钮。
- 写摘要时点「停止」会直接取消，不会一直转圈。

## 0.16.10

- **修：从别的 Agent 导入到 Codex 的会话，在 Agent 里显示成一大堆乱码。** Codex 可以导入 Claude Code 等的会话，
  导入时对方的每一次工具调用和工具输出，都被写成一段带标记的文字（`[external_agent_tool_call: Bash] … [/external_agent_tool_call]`、
  `[external_agent_tool_result] … [/external_agent_tool_result]`），AllAi 以前把它们原样当成回复正文和思考过程：
  一条回复 2.7 万字，全是命令、目录列表和脚本输出，输出里还有 GBK 被当 UTF-8 读出来的乱码。
  现在工具调用变成执行过程里正常的「运行命令 / 读文件 / 改文件」一行，工具输出不显示（和其它会话一样），只留真正写给你的话。
  以 `codex resume 01a07150-…` 那条为例，第一条回复从 26,942 字变回 1,067 字。手机上看也一样好了。

## 0.16.9

- **修：设置里切到「远程」会闪一下。** 对话框的高度以前跟着内容走，「远程」页一打开先显示一行「读取中…」再撑开，
  对话框先缩成一小条再长回来，后面的页面露出来，像是瞬间透明了一下（Agent 页在后面时内容多，最明显）。
  现在对话框固定高度，远程页也记住上次读到的配置。逐帧抓屏确认：切换过程中没有中间帧，也没有卡顿。
- **使用统计可以显示精确数字。** 右上角「简略 / 精确」切换，记在本机：简略是 2.6K、1.23M、10.00B，精确是 2,614、10,000,000,000。
  以前超过一百万只会写 M，一百亿会写成「10000.00M」；现在多大都能写全，卡片里放得下（太长会自动缩小换行）。
- **「未知模型」查清楚了：是 claude-sonnet-5。** 那是 9 月 9 日一轮 Claude 账号聊天，当时选的是「默认型号」，
  CLI 的结果里没带型号，AllAi 就记成了空。已经把那条记录补上；以后每一轮都从回复里读真实型号。
  实在没有型号的旧记录显示「未记录型号」。
- **统计行更详细，而且能自己选显示什么。** 新增缓存命中率、输入 / 输出、当前模型和它来自哪个服务；
  设置 → 通用 →「输入框下的统计行」里点选要显示的项目。
- **设置 → 模型与接口 → 生图视频：去掉「创作台生图模型」「创作台视频模型」。** 创作页里自己选模型。
- **Agent 的代码改动带上行号。** 展开一个文件，每一行左边是原文件的行号、右边是改完后的行号，每段前面标「第 N 行起」。
  Claude Code 用它工具结果里自带行号的补丁（最准）；Codex 的 diff 本来就有；Grok 的记录里没有行号，就去当前文件里找改完的那一段，
  找不到（后来又被改过）或者不止一处就不标，宁可不显示也不标错。实测最近的改动里：Claude 33/33、Codex 296/299、Grok 437/879 有行号。
- **聊天空白页多了一句「XX 愿意为您提供帮助」**（XX 是当前选的模型）；**每条对话最前面显示「使用 XX 开始对话」**，
  是第一条回复用的模型 —— 发第一条之前怎么换模型都不会进历史。

## 0.16.8

- **长对话快速跳转。** 聊天、Agent、创作的消息区左边多了一列短横线，一格是你发过的一句话（创作是一次生成），
  当前看到的那一轮高亮。鼠标移上去展开成列表，点哪条跳到哪条；离得远直接到位，近的平滑滚过去。窗口太窄时不显示。
- **Agent 历史能看到改了哪些代码。** 每条回复下面列出这一轮改了哪些文件和 +N −M，点开看具体加了什么、删了什么
  （绿色加、红色删，带上下文）。Claude Code、Codex、Grok Build 三家的会话记录都能解析；执行过程里改文件的那一步也带 +N −M。
  很长的会话里只给最近 40 条消息留具体内容，更早的只留统计 —— 实测带全量差异会让会话大小翻一倍，盯文件时界面会跟着卡。
- **Claude Code 在线状态重做。** 以前只看会话文件最近 3 分钟有没有写，放在后台闲着的会话就显示成关了。
  现在读 Claude Code 自己登记的进程文件（`~/.claude/sessions/<进程号>.json`）：进程还开着就是「在线」（绿点），正在干活是「运行中」（绿点闪）。
- **修：打开一条对话不是停在最新一条。** 手机网页上从列表点进对话会停在前面；电脑上在上一条对话往上翻过，再换一条也会停在中间。
  现在打开或换对话一律到最新一条，往上翻着看时新内容进来也不会把你拽下去；图片、代码块晚一点撑高时也跟到底。
- **设置整理。** 「聊天模型 / Agent 接口 / 生图视频」收进「模型与接口」，点进去是二级菜单。
- **设置新增「关于」。** 软件信息和声明；列出电脑上装了的 Claude Code / Codex / Grok Build（版本、路径），可以单个或全部更新。
  **每次启动 AllAi 会在后台自动更新所有 CLI**（用各家自己的 update 命令，启动 20 秒后开始，不影响使用），关于页里可以关。
- 左上角副标题改成「高效地与AI工作」。
- 手机网页和中继已更新部署。

## 0.16.7

- **全局搜索。** `Ctrl+K` 或侧栏底部「搜索」打开，聊天 / Agent / 创作一起搜，回车跳过去。标题和预览立刻出结果，聊天正文和创作提示词稍后补上。
- **Agent 做完发 Windows 通知。** 窗口没在前台时提醒；点通知会把窗口唤回来。设置 → 通用里可关。
- **官方额度到 80% / 90% 提醒。** 近 5 小时和近 7 天窗口，文案写「已用到」，不写剩余。可关。
- **手机各自看不同的 Agent 工作。** 打开一条工作只改这台手机自己的视图，电脑当前选中的那条不动。从手机发任务仍会让电脑切到那条（发送还是走界面）。

## 0.16.6

- **修：Agent 打字仍然会顿。** 上次只做了 memo，没有按真实耗时去量。量出来两件事：
  1. Agent 页每 4 秒在主进程同步跑一次 Windows `tasklist`（列出全部进程），这台机器上要 **430–460ms**，窗口会跟着卡一下；
  2. 输入框每敲一个字都 `setState` 到 `ChatApp`，整页跟着重绘。
  现在扫描不再跑 `tasklist`（改看会话 recency），扫描从 ~440ms 降到 **12ms**；输入文字留在输入框自己内部，聊天 / Agent / 创作三个专区打字都不再抬到整页。

## 0.16.5

- **修：Agent 专区打字偶尔一卡一卡。** 和 0.13.0 聊天是同一类问题：输入框在 `ChatApp` 里，每敲一个字整页重绘，Agent 消息没有 `memo`，Markdown 被重新解析。创作专区每一轮也漏了。现在 Agent 气泡/列表、创作每一轮都 memo；远程快照不再跟着输入框重算；Agent 历史 4 秒扫描如果内容没变不再触发重渲染。

## 0.16.4

- **手机网页补上电脑端常用操作，并加上过渡动画。** 聊天/Agent 可以开关联网、调推理强度；Agent 可以改权限模式。列表长按（或系统菜单）能重命名、删除对话、工作和创作。底部弹层、进入对话、提示条有过渡；尊重系统「减少动态效果」。
- 电脑端和中继都要更新：新操作走远程白名单。操控电脑、管理员身份仍只在电脑上用。

## 0.16.3

- **修：Codex 同一条工作会拆成多个 rollout 文件，AllAi 只打开其中一份，历史像没同步。** 列侧栏时按会话 id 合成一条；打开时按时间把各文件拼起来，中间重复的那句用户话只留一次。盯文件也会看同一目录里后来新写的 rollout，避免还在跑却盯着已经不写的旧文件。
- Codex 是否在跑，同时看这些文件的写入时间和 sqlite 里的 `recency_at_ms`（思考几分钟没落盘时，只看 3 分钟文件时间会误判成停了）。

## 0.16.2

- **重做移动端远程界面基础体验。** 增加层次化背景、触控反馈和更清晰的面板视觉基底。
- **手机端导航与电脑端视图解耦。** 手机打开聊天、Agent 或创作时，不再强制切换电脑当前专区。
- **降低远程页面切换开销。** 只挂载当前移动端专区，避免三个大型面板同时渲染。


## 0.16.1

- **手机网页加了「扫码配对」。** 首页和左上角切换电脑的菜单里都有，点开直接用摄像头扫电脑上的配对二维码；
  相机用不了时可以从相册选一张二维码截图。只在手机/平板（触屏）上显示，电脑浏览器里不出现。
  iPhone 的 Safari 不支持浏览器自带的条码识别，所以打进了 jsQR 自己解码。中继放开了本站使用相机的权限。
- **修：iPhone 上把网页加到主屏幕当 App 用，杀掉后台就要重新配对。** 0.16.0 把配对密钥作为
  CryptoKey 对象存进 IndexedDB，WebKit 的主屏幕 App 里这种对象在进程重启后读不回来，
  整张表读取失败，页面就当成从没配过。现在密钥存成普通字符串，IndexedDB 和 localStorage 各存一份，
  任何一份还在就能用；读到坏掉的旧记录会自动清掉。本机测过「重开页面」和「IndexedDB 整个丢了」两种情况都不用重配。
- 注意：iPhone 主屏幕 App 和 Safari 的存储是分开的，在 Safari 里配的对带不进主屏幕 App。
  **在主屏幕 App 里用新的「扫码配对」配一次**就行（0.16.0 配过的这一次也需要重配）。
- 需要重新部署中继：`npm run remote:deploy`。电脑端不用改设置。
- **修（中继/手机网页，不用装新包）：iPhone 主屏幕 App 一直显示旧版本，杀后台也不行。**
  服务器给了 no-cache，iOS 主屏幕 App 照样用缓存里的旧 `app.js`。现在每次打包带一个编号：
  页面里写成 `app.js?v=编号`（地址变了旧缓存就不作数），页面本身和 `version.json` 改成 no-store；
  手机网页启动和从后台切回来时会对一下服务器上的编号，旧了自己刷新一次（同一编号只刷一次，不会死循环）。

---

## 0.16.0

- **新功能：远程控制。** 在手机浏览器里操控电脑上的 AllAi，**聊天、Agent、创作**都能用。
  手机操控的就是电脑上正在运行的界面：手机上打开一条对话，电脑屏幕也切过去；
  两边发的消息、Agent 的执行过程、流式回复都同步显示。
- **经你自己服务器上的中继转发，端到端加密。** 扫码配对时交换一把这台手机专属的密钥
  （二维码里的密钥在链接 `#` 后面，不会发给服务器；10 分钟过期、只能用一次），
  每次连接再派生新的 AES-256-GCM 会话密钥，帧带顺序号 —— 重放、调换、篡改都会被识破。
  中继只经手密文，不存任何东西。电脑上的密钥和中继口令用 Windows 凭据加密保存。
- **多设备**：每台手机/平板单独配对、单独移除；手机上也可以配多台电脑切换。
- **远程权限**：Agent 默认沿用电脑上的权限模式，可以收紧（「全部放行」降为「接受编辑」）；
  管理员身份默认**不**开放给远程；「操控电脑」不开放给远程。
- 标题栏多了一个手机图标：开着远程时显示，有设备连着变绿并显示台数，点它打开「设置 → 远程」。
  设置里能看最近的远程操作（只记哪台设备、什么时候、哪类操作，不记内容）。
- 手机网页：深浅色跟随系统，可「添加到主屏幕」；图片上传/查看、创作出图、Agent 新建工作都能在手机上做。
- 部署步骤（Docker 或 systemd + 1Panel 反向代理）见 `docs/REMOTE.md`；中继用 `npm run remote:build` 打包。

---

## 0.15.5

- **修：提示条先从左边出来、再「瞬移」到中间。** 提示条靠 Tailwind 4 的
  `-translate-x-1/2` 居中，它写的是 CSS 的 `translate` 属性；入场动画却写
  `transform: translate(-50%, …)`。两个属性叠加，动画期间等于往左挪了两次，
  动画一结束 transform 撤掉就跳回中间。现在动画只动纵向。实测 150 帧里水平中心偏移恒为 0。
- **操控电脑的过程不再冒充用户说话。** 循环每一步都会替你给模型发一句
  「已执行…这是执行后的屏幕」，以前这些都显示成你的气泡。现在同一轮任务打上同一个编号，
  界面上只显示你打的那句目标 + **一条回复**：模型每一步说的话、要执行的动作和它当时看到的
  截图都收在「操控电脑 · 执行过程（N 步）」里，结论放在下面。发给模型的上下文不变。
  对话标题也用你的原话，不再是「目标：…已锁定窗口…」。
- **统计行有了整条底色，不再和消息叠在一起。** 以前只有中间一栏、没有底色，还往上顶了 4px。
- **每日热力图重新设计。** 格子改成按列宽撑满整张卡片（以前是写死的像素，季度视图右边空一大片）；
  统计改成下面一排小卡片：共计、活跃天数、活跃日均、单日最高（带日期）、连续使用（带最长）。
  悬停某天时第一张卡片显示那天的用量。「月」改成日历排布，格子里直接写日期和用量。

---

## 0.15.4

- **输入框的「联网 / 管理员 / 操控电脑」只留图标。** 带文字时会把发送键挤到下一行；
  现在是和附件按钮一样大的方形图标，1400 和最窄 980 宽下都和发送键在同一行。
  鼠标悬停出现软件自己样式的说明（不用系统 title 提示框），说明里带当前开/关状态。
- **修：管理员模式启动不了。** 三个问题叠在一起：
  1. 主进程拉起管理员宿主、终端宿主去连，两边拼出来的管道名不一样，永远连不上；
  2. 提权进程开的管道，默认只给 Everyone 读权限 —— 没提权的 AllAi 写不进去，
     UAC 点了「是」照样被拒；
  3. 失败时开关静默返回，等 60 秒按钮还是关的，没有任何提示。
  现在方向反过来：AllAi 开管道（随机名字 + 一次性密钥），提权宿主连回来报密钥，
  对上才收养、随即停止监听；整套逻辑搬进终端宿主。**没有**用 `writableAll` 图省事 ——
  那会让这台机器上任何账号都能借管理员宿主跑任意命令。
  开关打开时会提示「请在系统弹窗里确认」，失败会显示原因；UAC 点「否」立刻提示，
  不再等 60 秒。宿主在链路断开后自行退出，不留后台提权进程。
- 自动测试 20 项全过（错密钥冒充者被拒且拿不到任何回复、收养后新连接进不来、
  stdin/stdout 含中文、并发不串台、kill、无孤儿进程、重连、提权脚本引号和退出码）。
  **真实 UAC 弹窗那一步无法自动测，需要手动点一次确认。**

---

## 0.15.3 — 2026-09-10

**聊天和 Agent 输入框增加「管理员」开关；Agent 专区也可以操控电脑。**

- 「管理员」打开后弹一次系统 UAC，之后本机 CLI（官方登录聊天和 Agent）经提权宿主执行。AllAi 本身已是管理员时直接跑，不再多弹一次。第三方 HTTP 聊天不受影响。聊天和 Agent 各记一份开关。
- 管理员宿主 `admin-host.js` 放在 `resources/` 真实文件里，用 named pipe 回传 stdout/stderr。安装包检查会启动这份宿主并让它执行一条命令。
- Agent 输入框同样有「操控电脑」和确认横幅。视觉模型仍用聊天里选中的第三方接口；官方登录那条路看不到截图，开关是灰的。

## 0.15.2 — 2026-09-10

**修：操控电脑第二步起经常空转、复杂窗口上越用越乱。**

三处叠在一起，看起来就像整个功能不能用：

- 输入宿主超时后会把**下一句命令的回包**错配给上一句。浏览器这类窗口的 UI Automation 全树扫描很容易超过 10 秒，后面的点击/截图就开始对错位。
- `send()` 用渲染时的 `streaming` 闭包判断忙闲。循环里第一步刚结束就立刻发第二步时，可能拿到「还在流式」的旧函数，直接空返回，表现为「模型没给出动作」。
- 模型把动作写在普通 JSON、或包一层 `{action:{...}}` 时解析不出来。

现在每条命令带编号对回包；控件扫描只查按钮/输入框等类型；`send` 改走 ref。锁定目标时跳过任务栏和桌面，窗口矩形用可见边框，少点到阴影上。

## 0.15.1 — 2026-09-10

**修：0.15.0 打开操控电脑后立即提示“截屏失败”并停止。**

0.15.0 把控制范围绑定到了目标窗口，但截图仍依赖 Electron `desktopCapturer` 返回的
窗口源，再用窗口编号或标题去反查目标。一些窗口的源编号/标题并不稳定；目标已经锁定成功，
这里仍可能匹配不到。与此同时，截图、IPC 和上传三段错误被收成同一个 `null`，界面只能说
一句没有诊断价值的“截屏失败”。

现在 PowerShell/Win32 宿主直接按已经验证的 HWND 物理矩形抓取目标窗口，再缩放到
1600×1200 以内传回 Electron。它不再依赖窗口源 ID 或标题，跨显示器和不同 DPI 也使用同一套
物理坐标。窗口截图、IPC 调用和上传失败分别保留具体错误；PowerShell 输出固定为 UTF-8，
中文错误不会再乱码。

排查时还发现目标窗口重新聚焦的输入线程连接少了一段：原来只连接宿主与当前前台线程，
现在同时连接当前前台线程与目标线程，并加 Windows Shell 激活和短时重试。若其它程序仍然
主动抢走焦点，运行器会停止而不是盲目输入。

回归验证用一个隔离的 WinForms 目标窗口直接跑真实宿主截图，修复前稳定失败；修复后连续
3 次得到 960×630 的有效 JPEG（28–30KB）。应用/Electron 两套 TypeScript、ESLint、客户端
依赖检查、生产构建和安装包运行时检查也全部通过。

## 0.15.0 — 2026-09-10

**重构操控电脑：提高定位准确率、减少模型轮次，并修正任务执行到一半反复叫用户确认的问题。**

- 发送任务后 AllAi 会最小化，锁定它后面的目标窗口。此后只截目标窗口，截图提高到最高
  1600×1200；不再拿 1280 宽的整个主屏让模型猜位置。每次鼠标或键盘动作前都会重新激活
  并核对窗口句柄和进程，坐标越界、窗口被切走或目标关闭时直接阻止执行。
- 接入 Windows UI Automation。截图同时附带按钮、输入框、菜单项等可访问控件的名称、类型和
  临时编号，模型优先按编号调用控件；自绘界面再回退到截图坐标。也能看到其它已打开窗口并
  显式切换，跨应用任务不再依赖点击任务栏猜窗口。
- `text` 动作可一次完成“聚焦控件/点击输入框 → 输入 → 回车提交”，少跑一到两次模型。
  执行后的画面会做低成本变化检测；几乎没变化时明确要求模型重新定位，避免重复盲点。
  固定 700ms 等待改成按动作类型等待，移动、输入和滚动明显更快。
- 修正默认确认逻辑。用户发送的原始任务就是对完成该任务所需步骤的授权；明确要求的点击、
  输入、发送、删除、关闭等会连续完成。只有操作对象或金额等关键信息不清、动作超出原始要求
  时才显示确认横幅。“逐步审核”仍可用于调试，“完全自动”仍可关闭所有确认。
- 模型动作 JSON 会从助手正文中移除，改成可读的“准备执行/完成”步骤。模型不返回动作、连续
  执行失败、截图失败和达到步数上限也会写进对应历史消息，不再用白色弹窗报告 AI 工作过程。
- 打包验证会真正启动 `resources/computer.ps1`，同时检查 UTF-8 BOM、物理屏幕尺寸和
  UI Automation 是否可用，避免源码能跑但安装版输入宿主失效。

验证：客户端服务端依赖检查、应用/Electron 两套 TypeScript、ESLint、13 条动作协议测试、
PowerShell 宿主与 UI Automation/窗口枚举测试、Next 生产构建和安装包运行时检查。

## 0.14.4 — 2026-09-10

**修：AI 能看到截图、也能给出动作，但电脑没有任何反应。**

打包后 `computer.ps1` 有两份：一份在 **app.asar 里面**（`electron-dist` 被整个打进去了），
一份在 `resources/` 下（extraResources 复制的真实文件）。查找顺序是 `__dirname` 优先，
打包后它指向 asar 内部。`fs.existsSync` 对那个路径返回 **true** —— Electron 给 Node 的
fs 做了 asar 虚拟化 —— 但 **PowerShell 不认 asar**，`-File` 直接报「文件不存在」。
于是输入宿主根本起不来，每个动作都失败，模型只能一直说话。

开发时不会暴露，因为那时它就是磁盘上一个真实文件。**这是我打包验证的漏洞**：
之前只确认了 `resources/computer.ps1` 存在且带 BOM，没验证运行时**实际选中的是哪一份**。

改成优先取 `resources/` 下的真实文件，并显式跳过任何含 `.asar` 的路径。
`computerInfo()` 现在会返回解析到的脚本路径，以后出这类问题先看它。

**顺带**：动作连续失败两次就停下并把原因显示出来，不再让模型空转烧 token
（这次的现象「只能说话不能操控」就是被这个掩盖了）。

---

## 0.14.3 — 2026-09-10

**修：操控电脑一发送就说「模型没给出动作」——这次是真的根因。**

`runComputerLoop` 是个 `useCallback`，依赖里没有 `send`，所以它抓住的是**首次渲染
那一版的 `send`**。那时候 providers 还没加载完、`modelKey` 是空字符串，于是 `send`
在 `if (!key) return` 就掉头走人 —— 一个请求都没发出去，循环自然拿不到动作。
（它还会顺手把设置弹窗打开，这也是当时的一个线索。）

0.14.2 修的那个渲染竞态是真 bug，但不是这次的主因。

为什么我的测试没抓到：假模型环境里 prefs 是异步加载的，加载完依赖变化、回调被重建成
带正确 `modelKey` 的新版本；而真实环境里 prefs 恰好等于默认值，依赖没变，
坏闭包就一直用下去了。

现在 `send` / `prefs` / `modelKey` 一律走 ref 读最新值，`useCallback` 的依赖里
只留真正稳定的东西。用真实接口 + 默认的 GPT-5.5 验过：截图 → 模型返回
`{"op":"click","x":160,"y":101}` → 执行，整条链路通。

**顺带查清楚的**（都不是问题，排除掉了）：服务端 `/api/chat` 的流式路径正常，
截图确实变成了 `image_url` 送达模型，grok-4.6 / gpt-5.5 / gpt-5.6-sol 三个模型
都能正确识图并按协议返回动作块。

---

## 0.14.2 — 2026-09-10

**修：操控电脑一发送就说「模型没给出动作，已停下」。**

两个原因，都修了：

1. **竞态（真 bug）**：模型的正文只存在 React state 里，而循环在 `await send()` 之后
   立刻去读 `activeRef.current` —— 这时最后一次渲染不一定已经提交，读到的是**空**的
   助手消息，于是判定「没有动作」。假模型测试时一次性吐完整段侥幸赢了竞态，
   真模型逐字流式就输了。现在 `send()` 直接返回这一轮的正文，不再依赖渲染时机。
2. **模型没按格式回**：以前直接放弃。现在会明确提醒一次「只回一个 action 代码块」，
   再不给才停；而且停下时会把**模型的原话**带出来，不再是一句没用的「没给出动作」。

---

## 0.14.1 — 2026-09-10

- **修：官方登录的模型选中时，「操控电脑」开关现在是置灰的。** 那条路走本机 CLI 的
  单轮 print 模式，**收不到图片** —— 模型看不见屏幕只会瞎猜，开了也是走进死胡同。
  鼠标悬停会说明原因；万一绕过去了，循环开头也会拦一次并提示换成第三方视觉模型。

---

## 0.14.0 — 2026-09-10

**新增：操控电脑（Computer Use）。** 聊天输入框里多了一个开关，打开后发一句话，
AI 会截屏、看图、操作鼠标键盘，一步一截图直到做完。

怎么实现的：

- **看**：Electron 的 `desktopCapturer` 截整屏，缩到 1280 宽转 JPEG（约 110KB）。
- **动**：常驻一个 PowerShell 宿主，P/Invoke 调 user32（`SetCursorPos` / `mouse_event` /
  `SendKeys`）。**没有引入任何原生模块** —— robotjs / nut.js 每次升 Electron 都要重编译，
  这个项目被 node-pty 折腾够了。实测每个动作 0–5ms。
- **想**：截图 + 动作协议交给视觉模型，它回一个 ```action JSON 块，执行完再截图。
  用 JSON 块而不是 function calling，是因为第三方网关对 tools 的支持参差不齐
  （联网那次已经领教过，见产品约定 21），而读图是所有视觉模型都会的。
- 支持 click / move / drag / scroll / text / key / wait / foreground / done。

安全上做了这些：

- **三档**（设置 → 通用）：每步都问 / **危险动作才问（默认）** / 全自动。
  点击滚动算安全，输入文字、按键、拖拽、右键算危险。
  `Ctrl+S`、`Alt+F4`、`Delete`、`Ctrl+A` 这几个**无论哪一档都会停下来问**。
- 确认走输入框上方的横幅，**不弹窗** —— 弹窗会挡住屏幕，而这功能恰恰要你看着屏幕判断。
- 单次任务有步数上限（默认 15 步），随时可以按停止。
- 每一步的截图都存进对话，出了问题能回看 AI 当时到底看到了什么。

**踩到并修掉的坑：**

- **DPI**：这台机器缩放 1.5×，Electron 报 1707×1067（逻辑坐标），user32 实际是
  2560×1600（物理像素）。直接用会整体偏移三分之一。而且 `SetProcessDPIAware()`
  必须在加载 WinForms **之前**调，否则静默失败。
- **中文乱码**：PowerShell 5.1 按控制台码页（GBK）读 stdin，UTF-8 的中文直接变乱码。
  文本改成 base64 过管道，JSON 行保持纯 ASCII。
- **`.ps1` 必须带 UTF-8 BOM**：没有 BOM 时 PowerShell 5.1 按 ANSI 解，
  中文注释会被解成乱码字节，只要凑巧出现一个引号整个脚本就解析失败。
- **截图会累积**：每轮都往对话里加一张图，第 4 轮就在发 4 张。发给模型的那份现在
  只留最新一张（对话记录里仍然全都在），否则 15 步的任务光图就上万 token。

## 0.13.0 — 2026-09-10

**性能：打字从 145ms 一个字降到 6.7ms。**

用户反馈「莫名有点卡顿，甚至输入都会」。用一条 240 条消息 / 17 万字的对话实测，
每敲一个字符要 **145ms**（中位数），25 次输入全部触发长任务 —— 也就是每个字都掉帧。

CPU profile 指出两个来源：

1. **整个组件树没有一处 `memo()`。** 输入框每敲一个字 `setDraft` 一次，
   ChatApp 整棵树重渲染，`react-markdown` 把 120 条助手消息**从头重新解析一遍**
   （micromark 的 `go` / `consume` / `syntaxExtension` 占了采样的一大半）。
2. **输入框自适应高度用的是强制同步重排**（先把 `height` 设成 0 再读 `scrollHeight`），
   而文档里挂着上百段渲染好的 Markdown。这一下占 13.3%。

改动：

- `Markdown`、`UserBubble`、`AssistantBubble`、`MessageList` 全部 `memo()` 化。
- 配套把传给它们的回调稳定住：`send` / `editUserMessage` 每次渲染都是新函数，
  直接传下去 memo 就白做了，改用 latest-ref 拿到稳定 identity。
  `UserBubble` 的 `onEdit` 也从「每条消息现造一个闭包」改成收 `(id, content)`。
- 输入框高度交给 CSS 的 `field-sizing: content`（Chromium 123+ 原生支持），
  一行 JS 都不用跑；只有不支持的浏览器才退回老办法。
- 「这条和上一条助手消息换模型了吗」原来每条消息都往回扫一遍（240 条 ≈ 上万次比较、
  且每次渲染重算），改成一次遍历预先算好。
- 用量汇总和上下文占用改成 `useMemo`。用量记录是只增不删的（产品约定 25），
  以前每次渲染都从头遍历，几个月后必然变卡。

实测（同一条 240 条消息的对话）：

| | 修前 | 修后 |
|---|---|---|
| 打字延迟（中位） | 144.7ms | **6.7ms** |
| 打字延迟（p90） | 168.9ms | **6.8ms** |
| 25 次输入的长任务 | 25 | **0** |
| 同样 15 次输入的 CPU 采样 | 5,994 | **194** |

流式输出实测中位帧间隔 3.3ms、p95 7.9ms、零长任务。
发消息、改消息、切换对话都验过没被 memo 挡住更新。

## 0.12.3 — 2026-09-10

**修：Grok 的上下文占用虚高 6 倍。** 显示 `1.40M/500.0K · 279%`，
而 Grok 自己说只用了 4.5 万、约 9%。

0.12.2 我改成读 CLI 自报的数，但 Grok 那个字段挑错了。会话文件里
`turn_completed.usage.inputTokens` 是**一轮里所有模型调用的输入量之和**
（它旁边就写着 `modelCalls`），不是窗口占用。实测一轮 9 次调用：每次的 prompt 从
24,268 一路涨到 47,762，而 `inputTokens` 是这 9 次的总和 313,756。

真正的窗口占用是**最后一次推理的 prompt_tokens**，Grok 把每次推理都记在
`~/.grok/logs/unified.jsonl` 的 `shell.turn.inference_done` 里，带 `sid` 能对上会话。
现在从那儿读，同一条会话显示 `47.8K/500.0K · 10%`。

注意：`inputTokens` 作为**用量统计**是对的（那是真实计费量），只是不能当窗口占用。

**顺带修：聊天专区里 Claude 的占用会显示成个位数。** Anthropic 口径的
`input_tokens` **不含**缓存读（实测 input=2、cache_read=362818），而 OpenAI 和 Grok 的
**已经含了**。0.12.2 一律只取 `input`，对 Claude 就等于只算了没命中缓存的那点增量。
现在在解析时就按 wire 格式分开算，结果存成 `UsageEvent.contextTokens`；
老记录没有这个字段，读的时候按 `cacheRead > input` 现推。

**同样修了**：Grok 官方登录聊天一轮也可能调多次模型，占用改为同样从 CLI 日志里取。

## 0.12.2 — 2026-09-10

**修：上下文占用显示得离谱地高，而且会误触发压缩。**

用户报的现象：一条 Codex 会话，Codex 自己说用了 101,630 / 258,400（39%），
AllAi 显示 `456.7K/272.0K · 168%`。

根因是 0.12.0 把上下文占用**按整条会话的文字量估算**。这个估算看不见三件事：

1. CLI 内部早就 compact 过了 —— Codex 到约 200K 会自己压，压完窗口里只剩一小截，
   但会话文件里的原文一条都没少，我们照单全收地数了一遍；
2. 工具输出、系统提示、skills 这些没进消息列表、却真的占窗口的东西；
3. 各家分词器的差异。

**比显示错更严重的是**：这个虚高的数字还在驱动自动压缩。下一条消息发出去，
AllAi 就会判定「该压了」，丢掉一条只用了 39%、还带着十万 token 缓存命中的健康会话，
换成一份有损的摘要。

改成读 **CLI 自己报的数**（`electron/history.ts` 的 `readContextUsage`）：

- Codex：`event_msg/token_count` → `info.last_token_usage.input_tokens`，
  窗口大小取 `info.model_context_window`。**实测是 258,400，不是文档上的 272,000** ——
  现在以 CLI 报的为准，内置表只在 CLI 还没报过时兜底。
- Claude：`message.usage` 的 `input + cache_read + cache_creation`。
- Grok：`turn_completed` 的 `usage.inputTokens`。
- 聊天专区同理，用接口回报的上一轮输入量（`liveContextTokens`）。
- 手填的上限覆盖仍然优先级最高。全新会话 CLI 还没报过时，才退回字符估算，
  这时提示里会标「（估算）」。

修完同一条会话显示 `108.1K/258.4K · 42%`。

**顺带修：Grok Agent 的用量一条都没记上。** 它的一轮结束事件是
`params.update.sessionUpdate === "turn_completed"`，而且字段是 camelCase
（`inputTokens` / `cachedReadTokens`），两处都没认，于是 usage 被静默丢弃。
实测一条会话现在能正确记到 116,767。

## 0.12.1 — 2026-09-09

**稳定性与数据安全审计。** 这轮没有加新的大功能，集中修复会丢数据、丢上下文、
重复回答或把临时故障记成永久不支持的问题。

- **并行上传不再互相覆盖。** 原来的 `uploads/meta.json` 是无锁的读改写，40 个并发上传
  实测只有最后 1 个还能读取。现在元数据更新串行化、文件原子落盘；同样的 40 路回归
  全部可读。删除创作记录和角色图时也会清理对应文件，不再持续留下孤儿文件。
- **对话索引可自愈。** 会话文件已写好、程序却在更新 `index.json` 前退出时，旧版会让这条
  对话永久消失在侧栏。现在列表会核对磁盘文件并重建索引；损坏的索引和用量文件也不会
  再被当成空数据覆盖。对话的压缩摘要经整条保存接口后仍会保留。
- **联网失败分清永久与临时。** 只有 400/404/405/410/415/422/501 这类明确不支持才缓存；
  超时、断网、401、429、5xx 下一轮仍会尝试。Responses 已经输出一部分后断线时保留现有
  内容并显示内联错误，不再回退生成第二份答案接在后面。
- **三套 SSE 读取器都会消费没有结尾换行的最后一帧。** OpenAI 兼容流、Responses 流和
  前端流都修了，兼容一些直接关闭连接的中转站。
- **CLI 输出不再丢最后一行或拆坏中文。** Electron 侧改用 UTF-8 `StringDecoder`，实测把
  “最后一行😀”故意切在多字节中间且不加换行，仍能完整显示。
- **官方聊天重新生成会开新 CLI 分支并重放正确历史。** 不会续用已经包含旧答案的 session，
  也不会把当前问题重复拼两次。聊天发送前就失败的错误现在同样写进对话，刷新后还在。
- **上下文压缩按同一套有效消息计数。** 空错误消息和系统说明不再错移 `folded` 下标；
  emoji 截断也不会留下半个 UTF-16 代理项。摘要、标题、模型列表请求增加取消和超时边界。
- **补安全边界。** 外链只允许 HTTP、HTTPS、mailto；Agent 历史和托管 Skill 的删除都用
  规范化路径校验，避免相似目录前缀绕过。设置侧栏的全局项与第一个 Agent 不再同时高亮。
- **初始化改为局部容错。** 一项本地数据读取失败不再让整个应用初始化失败；连续提示也不会
  被前一个提示的旧定时器提前清掉。

验证：两套 TypeScript、ESLint、客户端边界检查、生产构建；40 路并发上传、索引恢复、
压缩保存、无尾换行 SSE、联网 503/422/半流中断、1463 组 emoji 截断及真实子进程分片输出。

## 0.12.0 — 2026-09-09

**按模型的真实上下文上限自动压缩，三个专区都有。**

每个模型能装的上下文差得很远，各家 CLI 到自家上限的 83%–85% 会自己 compact。
但 AllAi 的对话记录才是事实来源（换模型、换 CLI、走 HTTP 时都是我们把整段历史
重新喂过去的），CLI 内部压没压不算数 —— 得我们自己压。

- **内置上限表**（2026-09 查证）：Claude Opus 5 / Sonnet 5 1M、Haiku 4.5 与
  Claude 4 及更早 200K、Grok 4.6 500K、grok-build-0.1 256K、GPT-5 / Codex
  **272K**（标称 400K 里 128K 留给输出，只有 272K 能填输入 —— 按 400K 算会压得太晚，
  压的时候内容已经超过压缩接口本身能吃下的量了）、GPT-4.1 1M、Gemini 1M。
  认不出的型号一律按 128K 保守处理。中转站把窗口限小了的话，可以在设置里手填覆盖。
- **到量自动压**：早期内容压成一段交接记录，最近几轮保留原文（最多占上限 35%）。
  压完存在对话/工作/创作上，下一轮直接复用，不会每轮重新摘要。默认阈值 80%，
  可在「设置 → 通用」里调（50%–95%），也能整个关掉。
- **对官方登录的三条 CLI 路，压缩 = 放弃 `--resume`、用摘要开一条新会话** ——
  这正是各家 `/compact` 在做的事。旧会话里装的是没压过的全量，续下去只会更满。
  Agent 侧同理。
- **压缩过程显示在对话里**，不弹窗：聊天走消息里的说明行、Agent 走 trace、
  创作插一条和「换模型」同款的内联徽章。都写明「已到 X%、省下约 N token」。
- **输入框下的统计行现在显示 `已用/上限 · 百分比`**，接近阈值转黄、到了转红。
  聊天和 Agent 都有。
- **换模型交接和到量压缩合并成同一套逻辑**。以前交接写死「最近 6 条 / 12K 字符」，
  对 1M 窗口的模型是白白丢上下文，对 128K 的又可能不够；现在按目标模型的实际上限来。
- 一个能做摘要的第三方服务都没有时，退回「截断原文带过去」而不是什么都不给 ——
  什么都不给的后果是模型开始编，比丢细节严重得多。

**修：**

- **客户端组件引到服务端模块会让整个界面起不来**（`Can't resolve 'fs'`），
  而 tsc 和 eslint 都发现不了。加了 `scripts/check-client-imports.cjs`，
  挂在 `npm run build` 和 `npm run dist` 前面。

## 0.11.0 — 2026-09-09

- **窗口的最小化 / 最大化 / 关闭换成自己的**。以前那三个按钮是 Windows 画的，
  颜色跟着系统主题走，深色皮肤上顶着一条浅色的系统栏很出戏。现在窗口开
  `frame: false`，标题栏自己画：左边图标和版本号，右边三个按钮（关闭是红底），
  整条可拖动（按钮上设了 `no-drag`，不然点不动）。最大化状态由主进程推给界面，
  图标会跟着变；从系统那边（双击边框、Win+↑）改的也同步。
- **Agent 多了「全局提供商」**。三个 Agent 用的多半是同一个中转站，以前得在
  三个 Agent 里各填一遍 key 和地址，换中转站要改三次。现在「设置 → Agent 接口」
  最上面有一项「全局提供商」，在那儿配一次，三个 Agent 的接口列表里都会出现
  （带「全局」标记、只读，要改回那儿改），点「设为当前」就能用。
  key 只存一份在全局池里，**不会**复制进各个 Agent；但每个 Agent 可以在同一个
  全局接口下用不同的模型。
- **创作里的视频播放器换成自己的**。原来直接用 `<video controls>`，那是 Chrome
  自带的控件，长得和软件完全两个世界，而且全屏按钮点了没反应。现在播放/暂停、
  进度条、时间、静音、倍速（0.5×–2×）、下载、全屏全是自己的，点一下就生效；
  全屏是对整个播放器容器请求的，实测能进也能退（Esc 退出后图标会跟着换回来）。
- **使用统计加了 GitHub 那种热力图**，一列一周一格一天，颜色深浅是当天的 Token 量，
  跨度可选年 / 季 / 月（格子大小跟着变，一年 53 列也塞得下）。它有自己的跨度，
  不吃上面那个「7 天 / 30 天」筛选，只吃专区筛选。
- **修：使用统计里那个「(未知模型)」**。是官方登录的 Claude 记了一条 modelId 为空的
  用量。Claude Code 的 `result` 事件有时不带 `model` 字段，只在 `modelUsage` 这个
  map 的 key 上带；现在两处都读，并且界面这边还会拿当前这一轮的模型兜底。
- **修：`next build` 会 OOM**。追踪 `/page` 的依赖时会去读 `dist/` 里几百 MB 的旧安装包，
  报 `os error 1450`。排除规则原来只写了 `/*`（只匹配一层），补上 `/**`。

## 0.10.2 — 2026-09-09

- **Claude Code 正在跑也能认出来了，而且消息实时同步。** Claude 那边的 `running`
  一直被写死成 `false`（Codex 早就按文件新鲜度判断了，只有 Claude 漏了）。
  现在三家统一：会话文件 3 分钟内被写过就算在动 —— 实测正在跑的会话文件 3 秒前刚写过，
  第二新的是 13 小时前，区分度很足。**不扫进程名**（那会把 CC Switch 误报成运行中）。
  打开一条工作时会盯住它的会话文件，外部终端里说一句，AllAi 这边就跟着出一句。
- **创作里改生成类型（生图↔视频）现在也会记一条换模型。** 那两个类型用的是两个不同的
  设置项，模型确实换了，但以前只在「从下拉里选模型」时才记，所以这种换法在历史里完全看不出来。
- **删除 Agent 历史改用各家自己的删除命令**（`codex delete` / `grok sessions delete`），
  它们知道还有哪些索引要一起清。以前只删会话文件，codex 的 `session_index.jsonl`、
  grok 的 `active_sessions.json` 里还留着记录。命令不可用时退回删文件，
  并顺手清掉索引条目和删空后的空目录。两道安全阀（目录名/扩展名不符不删）保留。
- **所有删除确认框换成软件自己的样式**，不再是 Windows 的系统弹窗。共 6 处
  （删对话、删 Agent 工作、删创作、删服务、删 Skill、清空用量），红色危险按钮 + Esc 取消。

## 0.10.1 — 2026-09-09

**Agent 和创作也不丢上下文了。**（0.10.0 只做了聊天）

- **Agent**：换模型时和聊天一样会开新会话，以前只把当前这一句交给新模型，
  之前做过什么、定过什么全丢。现在按「摘要 + 最近几轮」交接，执行过程里会显示
  「接上下文：已把之前的进展交给新模型」。三个专区共用 `/api/context`。
- **创作**：以前**根本没有上下文**，连不换模型都没有 —— 生图接口是无状态的，
  「再来一张，换成夜晚」是当成一句孤立的提示词发出去的，模型不知道「上一张」是哪张。
  现在同一条创作里会带上：此前几轮的要求（文字）+ 上一张成品当参考图（视觉）。
  自动接着上一张改时，气泡下面会写「接着上一张改」，不做隐形魔法。

**顺带修好一个一直坏着的功能**：参考图的字段名写错了。发的是 `images[].url`，
而接口要的是 `images[].image_url`，直接 400 —— 也就是说「拖图当参考」「用作参考」
从来就没生效过。实测修正后返回 1.9MB 的成品图。

另外把创作 job 里存的提示词改回用户原话。之前存的是拼好上下文的整段，
下一轮又拿它当历史读回去，会一轮比一轮长，界面气泡里还会显示整段提示工程。

## 0.10.0 — 2026-09-09

**换模型不再丢上下文。**

以前：第三方接口每轮都把整条对话重放一遍，所以换模型不丢；
但官方登录那三条（Claude / Grok / ChatGPT 账号）只发当前这一轮 + `--resume <session>`，
记忆全在各家 CLI 自己的会话文件里。一换模型就是换 CLI，那个 session 根本不存在，
新模型从零开始 —— 而且它不知道自己失忆了，于是开始编。这就是记忆错乱和幻觉的来源。

现在的规矩：**AllAi 自己的对话记录是唯一事实来源，`--resume` 降级成纯粹的省 token 优化。**
每轮先比对三件事：上一轮是不是同一个官方账号、手上有没有它返回的 session、
我们记下的「已投喂条数」和现在的历史对不对得上。全中才续；任一条不中就开新会话，
把「摘要 + 最近 3 轮原文」重新喂给新模型。

喂的方式按各家能力选最好的那种：

- **Claude**：`--input-format stream-json` 从 stdin 喂真正的多轮，角色是原生的。
- **Grok / ChatGPT**：它们没有多轮输入，只能把历史写进提示词，
  并明确标注「这是历史、不是你此刻要回答的问题」。

摘要借任意一个有 Key 的第三方服务生成（官方账号没有 HTTP 接口）；一个都没有时
退化成截断原文，绝不会什么都不给。发生重放时会在对话里说明带了多少内容过去。

实测：一条 10 条消息的对话，暗号藏在被摘要掉的早期部分，
换到 Claude / Grok / ChatGPT 三家都答得出来；同模型继续聊仍然走 resume，不重复花钱。

## 0.9.17 — 2026-09-09

**修好视频生成一直超时。是软件的问题，不是中转的问题。**

对 `grok-imagine-video` 实测了整条链路，三个 bug 叠在一起：

1. **成片地址没解析到。** 任务完成时返回的是
   `{"status":"done","video":{"url":"/v1/videos/<id>/content"}}`，
   而代码只找 `url` 和 `data[0].url`，从来没看 `video.url` ——
   于是明明第 32 秒就出片了，还要一路轮询到 4 分钟报超时。
   现在按 `video/output/result/data[]/videos[]` 一圈找。
2. **地址是相对路径**（`/v1/videos/.../content`），直接 fetch 会失败。现在贴回服务域名。
3. **下载成片要鉴权**，不带 Authorization 返回 401。现在同域才带上 Key，
   跨域的 CDN 链接绝不带（别把密钥发给第三方）。

实测：38 秒出片，2.1MB 的 mp4，能正常播放。生图回归也过了。

## 0.9.16 — 2026-09-09

**创作重做成真正的聊天布局。**

之前那不是聊天，是个两栏表格：`grid-cols-[1fr_16rem]` 把图和提示词并排放在同一行，
所以图片顶边永远和气泡齐平。现在和聊天页一样上下排 —— 我的提示词在右，出来的图在左，
下一轮接着往下走。

顺带修了两个藏在里面的问题：

- **产出那一列没有确定宽度**（只写了 `max-w-`，会收缩到内容宽度）。后果是里面的
  `w-full` 没有参照：「正在生成」的占位框塌成 0 高（DOM 里有、屏幕上看不见），
  图片也按原始像素乱跳。改成 `w-full max-w-[75%]`。
- **图片没有高度上限**，一张竖图能顶穿一屏。现在按高度封顶 26rem，宽度随比例走，
  4:3 出 555×416、1:1 出 416×416，比例不会被拉伸。点开仍然能看大图。

## 0.9.15 — 2026-09-09

**创作：发出去立刻出气泡；图能放大下载；生成可以取消。**

提示词是右上角的小气泡，左边图更大。点图可放大、滚轮缩放，右键下载 / 复制 / 用作参考；也可以把图拖进输入框当参考。
发送后右侧马上出现提示词，左侧是正在生成。生成中途可以点停止。视频失败会带上接口返回的原因，不再只写「超时」。
换模型提示改成 `型号 · 来源 → 型号 · 来源`。

## 0.9.14 — 2026-09-09

**创作改成对话：一张图不再新开一条；去掉语音输入。**

创作侧栏和聊天一样是一条对话。同一窗口里连续出图、改提示词，都记在这一条里，点「新创作」才另开。
第一轮会自动总结标题。画面在左、提示词在右。
输入框上的麦克风和设置里的语音识别都拿掉了。

## 0.9.13 — 2026-09-09

**Grok 显示周已用百分比；ChatGPT / Grok 显示重置次数。**

Grok 的 `GetGrokCreditsConfig` 返回本周已用百分比和窗口起止，没有 5 小时窗口，也没有重置次数。
重置次数来自 `GetRemainingResets`。ChatGPT 重置次数来自 `wham/rate-limit-reset-credits` 的 `available_count`。
统计行例如：ChatGPT `5h 21% · 7d 5% · 重置 3`，Grok `7d 21% · 重置 1`。

## 0.9.12 — 2026-09-09

**ChatGPT 和 Grok 也能显示官方额度窗口。**

ChatGPT 账号走本机 Codex 登录令牌请求 `chatgpt.com/backend-api/wham/usage`，统计行显示 5 小时 / 7 天已用百分比。
Grok 账号走本机 `auth.json` 的 JWT 请求 `GetGrokCreditsConfig`，显示本周额度点数。
三家都从本机官方登录文件取令牌，不用再贴一遍。

## 0.9.11 — 2026-09-09

**Agent 也有统计行；点本地成品打开文件夹；Claude 官方额度已用百分比。**

设置里打开统计行后，Agent 输入框上面也会显示速度、上下文、本对话消耗。
历史里点 AI 写出的本地文件，会打开它所在的文件夹，不再丢给浏览器。
Claude 官号能读到 5 小时 / 7 天额度的已用百分比（走本机 curl）。Grok / ChatGPT 仍显示 AllAi 记下的已用 token。

## 0.9.10 — 2026-09-09

**Grok 历史被截断，Codex 干活时的旁白挤成一段。**

打开会话以前只读文件末尾 4MB、只留 80 条。Grok 的 `updates.jsonl` 动辄十几 MB，开头的对话就没了。
现在打开哪条就读哪条全文。Codex 每一段工作旁白不再首尾相接，按段分开显示。

## 0.9.9 — 2026-09-09

**Codex 桌面端开着，AllAi 侧栏却没有。**

以前进 Agent 页只在软件启动时扫一次，Codex 后来才开的会话当然看不见。
而且「运行中」只看 jsonl 三分钟内有没有改过；Codex 桌面端的活跃时间写在 sqlite 里，文件可能几分钟都不动。
现在开着 Agent 页会每 4 秒再扫：桌面端在跑、且这条会话 15 分钟内用过，就标运行中。标题优先用 Codex 自己起的短名。

## 0.9.8 — 2026-09-09

**Codex 历史扫出重复对话、消息写两遍。**

Codex 0.153 同一轮会写两份记录：`item_completed` 里一份，`response_item.message` 里再一份。
解析两边都收，用户在 Codex 里发的话在 AllAi 里就会出现两次。
另外审批用的 guardian 子会话也会被扫成一条独立对话，内容全是评估 JSON，看起来像代码。
现在用户对话只认 `item_completed`（老文件没有这项时再走原来的 message），guardian / 子 Agent 会话不再进列表。
正在写的会话（3 分钟内改过文件）会标成运行中。

## 0.9.7 — 2026-09-09

**换模型立刻出现在对话里。**

以前「从 A 切换到 B」是下一条回复来了才推断出来的，时间对不上。
现在聊天、Agent、创作台都是你一改模型就把这条提示写进时间线，刷新还在。

## 0.9.6 — 2026-09-09

**Agent 输入框可以选权限模式了。**

思考强度右边多了一个「权限」按钮。Grok / Claude / Codex 各用各家 CLI 自己的模式
（自动、接受编辑、计划、不询问、全部放行；Codex 是自动 / 只读 / 全部放行），
按 Agent 种类分开记住。默认还是以前的全部放行（Codex 是自动批准）。

## 0.9.5 — 2026-09-09

**Codex 官方登录发消息立刻报错。**

本机 Codex 0.153 起，`--sandbox` 和 `--approve-for-me` 不能一起用。
Agent 路径两个都传了，clap 直接拒：`the argument '--sandbox <SANDBOX_MODE>' cannot be used with '--approve-for-me'`。
`--approve-for-me` 本身就是 workspace-write 沙箱 + 自动批，去掉多余的 `--sandbox` 即可。
聊天里的 ChatGPT 账号仍是 `--sandbox read-only`，不受影响。

## 0.9.4 — 2026-09-09

**AI 干活的过程不再弹窗，全部显示在对话里。**

- 「联网搜索」「这次没有真的联网」「不支持联网」这些，现在是助手消息上方的一行小字，
  带图标，搜索进行中会呼吸。它们本来就是回答的一部分。
- **报错也进对话**，红色显示，而且**会跟着消息存下来** —— 以前报错走弹窗，刷新就没了，
  历史里看不出这轮为什么是空的。现在刷新后还在，旁边还留着「重新生成」。
- Agent 那边同理：出错和上下文重置显示在「执行过程」里，出错时面板默认展开，
  标题直接写「执行出错」。
- 剩下的弹窗只保留应用级操作（已删除、请选择工作目录、请在浏览器完成授权这类），
  顺手把样式改了 —— 之前深色模式下是个白块，现在跟着主题走。

## 0.9.3 — 2026-09-09

**Grok 联网真的能用了。**

xAI 把 `chat/completions` 上的 Live Search 下线了（传 `search_parameters` 返回
410「Live search is deprecated, please switch to the Agent Tools API」，
传 `tools:[{type:"web_search"}]` 返回 422，因为那个端点只认 `function` 和废弃的
`live_search`）。新版联网只在 **`/v1/responses`** 上。

- 聊天开了联网就走 `/v1/responses` + `tools:[{type:"web_search"}]`，不再往
  `chat/completions` 塞联网参数。实测 Grok 4.6 能答出 x.ai 首页此刻的真实标题。
- 搜索期间会提示「正在联网搜索…」。
- **接口收了参数但没真搜时会明说**：「这次没有真的联网（接口接受了参数但没执行搜索），
  答案来自模型自己的知识」。不让你把没查过网的答案当成查过的。
- 接口根本不支持 `/responses` 时退回不联网，并提示哪个服务能联网。
- 0.9.1/0.9.2 记下的「不支持联网」名单会清空一次重新试 —— 那批是按旧机制记的。

## 0.9.2 — 2026-09-09

- 联网不可用时的提示改成能照着做的：会直接告诉你换哪个服务能真的联网（例如 Grok 就提示改用「Grok 账号」官方登录）。

**关于第三方中转的 Grok 联网**：探过了，那个网关做不到。三种写法都试过 ——
`web_search_options` 和 xAI 官方的 `search_parameters` 都返回 410（明确拒绝），
`enable_search` 返回 200 但被静默忽略（带上它问「x.ai 首页现在写着什么」，
Grok 自己回答「我无法联网」）。这是网关本身没有这个能力，不是参数没写对。
要 Grok 联网只能走「Grok 账号」官方登录那条路。

## 0.9.1 — 2026-09-09

修 0.9.0 联网开关的两个问题。

- **开联网后 Grok 直接报错**（`模型接口 422`）。两个原因：
  1. 我往请求里多塞了一个 `tools: [{type:"web_search"}]`，那是个不可能生效的假工具，严格一点的网关直接拒。现在只发 OpenAI 文档里的 `web_search_options`。
  2. 失败后的兜底重试写得太窄 —— 只认 400、还要求报文里出现 `web_search`。中转网关会把上游错误包成 422/410，报文里也没这个词，所以从没触发过。现在带了联网参数又被拒，**任何状态码**都退回不联网重试一次。
- 支持与否**按模型记**，不按服务记。同一个中转地址上 GPT 能接受这个参数而 Grok 不能（实测确实如此），按服务记会误伤。
- 第一次被拒之后会记住，后面不再白试一遍，也不会重复打扰你。改了服务地址或模型列表会重置，重新试一次。
- 联网没生效时会提示一句「xxx 不支持联网搜索（410），这次按不联网回答」，而不是让你以为答案是查过网的。

## 0.9.0 — 2026-09-09

**修复**

- **OpenAI 模型不显示 token 速度**：不是模型的问题，是我的计时器写错了。那个 effect 依赖 `active`，流式时每来一个 token 就把 300ms 的定时器重建一次；HTTP 流几十毫秒一个 token，定时器永远等不到触发，所以恒为 0。Grok 官方登录是大块慢速更新，偶尔能撑过去，才显得「只有它正常」。改成用 ref 读最新值，只依赖 `streaming`。

**新功能**

- **联网搜索开关**，就在输入框里推理强度左边。聊天和 Agent 分开管，设置 → 通用 里也能改。
  - Claude 账号 → `--tools WebSearch,WebFetch`
  - Grok 账号 → `--tools web_search,web_fetch`
  - ChatGPT 账号 / Codex → `-c tools.web_search=true`
  - 第三方接口 → 带上 `web_search_options` 和 `web_search` 工具；网关不认就自动去掉重试一次，不会把这轮聊天弄挂
  - 创作台是生图，没有联网这回事，所以没给开关
- **模型图标**：所有选模型的地方（聊天顶栏、Agent、新工作、设置里的服务和模型列表）都会在名字前显示厂商图标。Anthropic / xAI / OpenAI 用内置矢量图；认得出牌子但没图的（DeepSeek、Kimi、GLM、通义…）和完全认不出的模型，可以在设置 → 通用 → 模型图标 里自己配 —— 填图片网址直接用，填域名（如 `deepseek.com`）会去抓站点图标，也能从本地选。抓到的存成 data URI 放本机，离线也显示。认不出又没配的就不画，不瞎猜。
- **创作历史可以改名和删除**了，删除会连生成的图片文件一起清掉。新创作会自动起标题（取提示词第一个自然停顿之前的部分），不再整段当标题。

**其它**

- 设置里「通用」挪到第一个（和大多数软件一样），后面按「接模型 → 配 Agent → 生图 → 技能 → 看用量」排。

## 0.8.1 — 2026-09-09

- 设置里新增**「通用」**标签页，0.8.0 加的两个开关搬到这里 —— 之前塞在「生图视频」页最底下，跟生图没关系，根本找不到。
  - 输入框下的统计行（开 / 关）
  - 麦克风按钮（自动 / 总是显示 / 隐藏）+ 语音识别模型
- 「生图视频」页回归只管生图和视频。

## 0.8.0 — 2026-09-09

**新功能**

- **输入框下的统计行**（设置 → 生图视频 → 统计行，默认关）：输出速度 tok/s、估算的上下文长度、这条对话累计消耗和轮数、CLI 上报的花费。用官方登录账号时还会显示该账号近 5 小时 / 近 7 天的用量。
- **官方账号用量**：设置 → 使用统计 顶部多一块，按 Claude / Grok / ChatGPT 账号分别显示滚动窗口内的消耗。
- **Agent 可以发图片了**：附件会先复制进工作目录的 `.allai-files/`，Agent 的沙箱才读得到（以前放在 `~/.allai/uploads` 是读不到的）。图片会明确告诉模型「请直接看图」，Codex 走它原生的 `-i` 参数。
- **麦克风按钮会自动隐藏**：没有可用的语音识别服务就不显示。设置里可以改成总是显示或彻底关掉。
- **创作台改成对话式**：右边是你写的提示词，左边是生成的图/视频，按时间从上往下排。点左侧历史会滚到那一次。

**关于「剩余额度」**

做不了，不是偷懒 —— 三家命令行都没有这个接口，我逐个查过：

- `claude` 没有 usage 子命令，`auth status --json` 只给 `subscriptionType: pro`，会话记录里也没有任何 rate limit 字段；
- `grok` 的子命令里没有额度相关的，`auth.json` 只有身份信息；
- `codex login status` 只回一句「Logged in using ChatGPT」。

所以显示的是 **AllAi 自己记到的用量**（近 5 小时 / 近 7 天），标题写的是「已用」而不是「剩余」。等哪家 CLI 开了配额接口再接。

## 0.7.0 — 2026-09-09

**「思考过程」重做**

以前那一栏其实是把工具调用的参数原样 JSON 打出来，所以：Claude 全是 `{"command":"…"}`，Codex 全是 `exec`。现在按工具类型翻成中文加图标：运行命令 / 读文件 / 改文件 / 搜代码 / 打开网页…，右边跟真正有用的那一项（命令本身、文件名、搜索词）。

- Codex 的事件以前根本没接住（真数据在 `item_completed.item` 里，PascalCase），现在命令、改文件、MCP、联网搜索都能正确显示。Codex 的思考正文是加密的，所以它那栏标题会显示「执行过程」而不是「思考过程」—— 名副其实。
- Grok 修了个真 bug：以前读的是 `hook_execution` 上的 `tool_name`，那是钩子不是工具，于是一个会话能刷出 611 条重复条目。现在读 `tool_call`，同一个会话 309 条且每条都有细节。
- Claude 的会话记录里思考正文是空的（只留签名），所以它那栏也如实叫「执行过程」。

**新功能**

- **ChatGPT 账号**（设置 → 添加服务）：走本机 Codex 登录的 ChatGPT 订阅额度，但界面是 AllAi 自己的，不再是内嵌网页。原来的内嵌网页改名「ChatGPT 网页版」保留，需要画布、图片那些网页独有功能时还能用。
- **可以改上一条发出去的消息了**：鼠标移到自己那条消息上，左边出现铅笔。改完回车重发，后面的回复会被替换。
- 语音识别可以在设置里**指定用哪个服务**，错了会明确告诉你错在哪，不再是「404 Page not found」。
- 录音时**边说边出字**：每 5 秒切一段转写，结果按段拼进输入框。

**修复**

- 官方登录聊天续不上会话时（CLI 那边 session 没了），现在会明确提示「这一轮从头开始，之前的上下文没带上」，不再默默丢上下文。
- ChatGPT 账号的聊天目录也排除出 Agent 列表。

**关于语音的说明**：桌面版用不了浏览器自带的语音识别。实测 Electron 里 `SpeechRecognition` 能录到声音，但识别要连 Google 的服务，直接报 `error: network`。所以只能走服务端转写，需要一个支持 `/audio/transcriptions` 的服务（你现在接的 ai.yp.mk 没有这个接口，Groq 有）。

## 0.6.0 — 2026-09-08

**新功能**

- **使用统计**：设置里新增一页，按聊天 / Agent / 创作分别统计 token —— 输入、输出、命中缓存、写入缓存、思考 token 分开看，带每日折线图、按模型的明细表、CSV 导出。数据存在 `~/.allai/usage.json`，只有点「清空」才会删。Claude / Grok 这类会上报花费的还能看到实际 $ 数。
- **复制恢复命令**：Agent 工作区右上角多了两个按钮 —— 一个复制工作目录，一个复制在官方终端里接着这条会话的命令（`grok --resume <id>` / `codex resume <id>` / `claude --resume <id>`）。
- **Agent 历史可以删和改名了**：侧栏工作条目悬停出现改名和删除。删除会连同该 CLI 自己的会话记录一起删（有确认框），删不掉就退回到只从列表隐藏。
- 聊天中途换模型，历史里现在显示「从 A → B」并带上两边的品牌图标，不再只写「已切换到 X」。
- Agent 新对话的空状态加了品牌图标，副标题改成「发送你的需求，让 Agent 开始工作」。

**修复**

- 新工作里选模型时列表被对话框裁掉半截：下拉菜单改成挂在 body 上，空间不够会自动往上翻，并且始终留在视口内。
- Grok 官方登录聊天的会话会跑到「本地 Agent」列表里（0.5.0 引入的）。现在 Claude 和 Grok 的聊天目录都排除掉了。
- Claude 工作的标题有时是 `<local-command-caveat>…` 这种命令包装文本，现在跳过。
- Agent 历史扫描的缓存落盘了（`~/.allai/history-cache.json`），重启后第一次进 Agent 页不用再全量扫（实测 39ms → 6ms）。

## 0.5.0 — 2026-09-08

新增 Grok 官方登录聊天，清掉交接文档里剩下的全部待办。

**新功能**

- **Grok 账号**：设置 → 聊天模型 → 添加服务 → Grok 账号 → 登录。和 Claude 账号一样走本机 CLI 和你自己的订阅额度，不碰第三方 Grok 接口。支持多轮续接、思考档位（low/medium/high/xhigh）。
- 官方登录聊天现在会自动生成对话标题（借任意一个有 Key 的服务总结），侧栏不再只显示你第一句话的前 28 个字。
- API Key 可以清除了：设置里 Key 旁边多了「清除已保存的 Key」，不用再删掉整个服务重建。

**重构**

- Claude 账号和 Grok 账号合并成一套「官方登录聊天」（`lib/official-chat.ts`），加第三个只要往表里加一行。
- 对话从 `db.json` 搬到 `~/.allai/conversations/<id>.json`，列表读 `index.json`。以前发一条消息要把所有对话的所有消息读写一遍。首次启动自动迁移。
- 官方型号表合并成 `electron/official-models.json` 一份，聊天、Agent、CLI 兜底三处共用（原来是三份拷贝）。
- 删掉 `claude:status/login/logout` 三个 Claude 专用 IPC，改用已有的 `cli:auth-*`。

**修复 / 优化**

- Agent 页每次打开都要把每个 session 文件读 96KB 头部，现在按 mtime+size 缓存。
- 打开一条 Agent 工作不再整份读入 jsonl（Claude 的能到几十 MB），只读末尾 4MB。
- 语音识别不再把每个网关 × 3 个模型全打一遍；鉴权失败或连不上就跳过该服务，总次数封顶 6。
- 上传的非图片/音视频文件不再以原始 MIME 内联返回，改为 `application/octet-stream` 附件下载并加 CSP sandbox。
- `/api/prefs` 只接受已知字段，脏 key 不再写进 db.json。
- Grok「运行中」判断加了时间边界，减少 Windows 上 pid 回收导致的误报。

## 0.4.20 — 2026-09-08

修 bug，没有新功能。

- 中文输入法选词时按回车不再直接把半截消息发出去。
- 服务的模型列表只要在设置里保存过一次，就归用户维护：启动扫描不再自动回填，清空了也不会被塞回来。手动点「从接口同步」不受影响。
- 启动扫描拉模型目录改到数据库锁外面。以前网关慢的时候，开机后最长 48 秒里所有接口请求都排在它后面。
- 本机 CLI 配置改成一个进程扫一次，不再每条聊天消息都读一遍 `~/.grok`、`~/.claude`、`~/.codex`。
- Codex 用 API Key 登录时 `.cmd` 被二次加引号（0.4.13 那个坑还留在登录这条路上）。
- Claude 的登录状态查询和退出登录也走 `cliInvocation`，不再直接 execFile。
- 终端宿主不回包时主进程不再无限期等待；认不出的请求也会回一句错误。
- AllAi 自己的「Claude 账号」聊天不再作为「工作」出现在本地 Agent 列表里。
- Agent 对话流式输出时往上翻不会再被拽回底部。
- 长对话流式输出时每帧重复扫描整个消息列表的开销去掉了。
- README 里的安装包版本号不再写死 0.1.0。

## 0.4.19 — 2026-09-08

- `grok-imagine` 识别为生图模型，不再出现在聊天模型列表。
- `grok-imagine-video` 仍为视频；`grok-imagine-image*` 仍为生图。

## 0.4.18 — 2026-09-08

- 启动扫描不再把本机 Grok/Agent 配置里的模型合并进已经保存过的聊天服务。
- 同一接口地址的 OpenAI 服务删掉 Grok 模型并保存后，不会再被自动塞回来。
- 「从接口同步」改为用远端列表覆盖当前列表，确认后点保存。

## 0.4.17 — 2026-09-08

- 从 Claude 官方聊天切到其它服务时，不再拿 Claude 的 session 去续。
- 第三方接口返回「模型不存在 / 该分组不支持」时，提示改到真正提供该模型的服务。

## 0.4.16 — 2026-09-08

- 修复幽灵对话：上一次 Claude 请求失败后，不再用 AllAi 自己的对话 ID 去 `--resume`。
- 只续 Claude 真正返回的 session；对不上就开新会话。

## 0.4.15 — 2026-09-08

- Claude Code `-p --output-format=stream-json` 补上 `--verbose`，修复官方登录聊天直接报错。

## 0.4.14 — 2026-09-08

- 去掉设置顶栏常驻的「Claude 账号」。
- 聊天里的 Claude 改为「添加服务 → Claude 账号」，和其它服务同一套入口。
- 聊天模型列表使用具体型号（Sonnet 5、Opus 5、Fable 5.1 等），不再只用 sonnet/opus/haiku 别名。

## 0.4.13 — 2026-09-08

- 修复 Windows 上 `codex.cmd` 被二次加引号，导致登录后拉模型失败。
- Codex 改为走真正的 CLI（unwrap npm shim）。
- Agent 官方登录的 Claude 模型改为具体型号，不再只显示 Claude Sonnet / Opus。
- 登录成功后模型同步失败，不再显示成登录失败。

## 0.4.12 — 2026-09-08

- 添加「官方登录」接口时，不再被写成第三方接口。
- 保存官方登录时不再套用其它接口上的 Key/地址。
- 用户选的 mode 为准：官方就是官方。

## 0.4.11 — 2026-09-08

- 官方登录成功后自动从 CLI 拉远端模型目录。
- Codex 使用 `codex debug models`（GPT-6 Astra、5.6 Sol/Terra/Luna 等），不再用写死的 gpt-5.4 / o3。
- Grok 使用 `grok models`。
- Agent 设置里官方接口可「从官方同步」。

## 0.4.10 — 2026-09-08

- Agent 官方登录与聊天 Claude 同一套状态：绿色「已登录」+「退出登录」。
- Codex / Claude Code / Grok 都会等到浏览器授权真正成功再更新按钮。

## 0.4.9 — 2026-09-08

- 聊天设置里 Claude 登录会等到 `claude auth status` 成功。
- 已登录显示绿色按钮，可退出登录。
- 修复授权后只提示「登录流程已结束」、界面不更新。

## 0.4.8 — 2026-09-08

- 启动扫描本机 Agent / Grok / Claude / Codex 配置，把发现的第三方 API 加进设置（同一 base URL 不重复）。
- 官方 OAuth 不含真实 API Key 的，不建成聊天服务。
- 思考强度标签对齐官方：Codex 轻量/中/高/极高/Ultra；Grok low/medium/high/xhigh。

## 0.4.7 — 2026-09-08

- 对话标题会在回复后即时总结，不再只重启后才更新。
- 聊天 Claude 登录放到设置里；登录后才出现 Claude 模型。

## 0.4.0 – 0.4.6

桌面聊天 + Agent 主线成型期间的连续小版本，安装包均在 `dist/`。主要包括：

- Next.js 16 + Electron 44 Windows 桌面壳、NSIS 安装包、桌面快捷方式。
- 多服务聊天：OpenAI 兼容 `/v1/chat/completions` + SSE，密钥在 `~/.allai/db.json`。
- Agent：Grok Build / Claude Code / Codex，官方登录与第三方 API 分开。
- 默认接口不再误标成「官方登录」；有 Key/地址的按第三方处理。
- 聊天与 Agent 的模型列表分开，禁止把聊天服务的模型塞进 Agent。
- 去掉进程扫描任意 `claude.exe` 造成的「Claude Code 运行中」误报。
- 去掉把历史塞进用户消息导致的角色错乱。
- 模型中途切换：若 CLI session 还是旧模型则开新 session。
- ChatGPT 网页额度、生图/视频工作室、Skills。

## 0.3.1

- 0.3 线的修补安装包。

## 0.3.0

- Agent 与桌面终端能力进入可用安装包。

## 0.2.0

- 桌面版能力扩展（安装包 `AllAi-Setup-0.2.0.exe`）。

## 0.1.0

- 首个可分发安装包：聊天窗口 + 本机密钥接入。
- `npm run dist` 生成 NSIS 安装程序并写桌面快捷方式。
